---
phase: 08-safe-sharing-and-public-access
plan: 03
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - frontend/src/app/core/tasks/task-library.models.ts
  - frontend/src/app/core/tasks/task-library.service.ts
  - frontend/src/app/features/library/task-shell-editor-entry.component.ts
  - frontend/src/app/features/library/task-shell-editor-entry.component.spec.ts
autonomous: true
requirements:
  - SHAR-01
  - SHAR-02
  - SHAR-03
  - SHAR-04
  - SHAR-05
  - MEDI-03
user_setup: []
must_haves:
  truths:
    - "Share management belongs in the authenticated editor because only owners can create, rotate, or revoke links."
    - "Share creation must stay tied to the saved task boundary and must not imply draft-state publishing."
    - "The editor should prepare the frontend service layer for later public read and duplicate flows without rebuilding Phase 7 playback UI."
  artifacts:
    - path: "frontend/src/app/core/tasks/task-library.service.ts"
      provides: "Frontend methods for share management plus reusable public-share API accessors for later plans"
      min_lines: 90
    - path: "frontend/src/app/features/library/task-shell-editor-entry.component.ts"
      provides: "Owner-facing share controls for view and present links with saved-state guidance"
      min_lines: 420
    - path: "frontend/src/app/features/library/task-shell-editor-entry.component.spec.ts"
      provides: "Coverage for share panel rendering, mode-specific actions, and saved-only gating"
      min_lines: 220
  key_links:
    - from: "frontend/src/app/features/library/task-shell-editor-entry.component.ts"
      to: "frontend/src/app/core/tasks/task-library.service.ts"
      via: "the editor manages share creation, regeneration, revocation, and copy-link actions through dedicated API methods"
      pattern: "share"
    - from: "frontend/src/app/features/library/task-shell-editor-entry.component.ts"
      to: "frontend/src/app/features/present/task-guided-present-page.component.ts"
      via: "share controls must describe reuse of the saved guided-present contract rather than launching a new session surface from draft state"
      pattern: "present"
---

<objective>
Add the authenticated owner-facing share-management surface in the editor and prepare the frontend task service layer for the public-share contracts introduced elsewhere in Phase 8.

Purpose: Users need an explicit place to create and control public links from the saved task they own. This plan keeps sharing anchored to the current editor route while preserving the explicit save boundary already used for media, preview, and guided present.
Output: A task editor share panel that can create, copy, rotate, and revoke `view` and `present` links, backed by service methods aligned with the new backend share-management contract.
</objective>

<execution_context>
@C:/Users/davmelis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/davmelis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-safe-sharing-and-public-access/08-RESEARCH.md
@frontend/src/app/features/library/task-shell-editor-entry.component.ts
@frontend/src/app/core/tasks/task-library.service.ts
@frontend/src/app/core/tasks/task-library.models.ts
@frontend/src/app/features/present/task-guided-present-page.component.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend frontend task services with share-management and public-share request methods</name>
  <files>frontend/src/app/core/tasks/task-library.models.ts, frontend/src/app/core/tasks/task-library.service.ts</files>
  <action>Add typed request and response models for task shares and implement service methods for list shares, create or rotate a mode-specific share, revoke a share, fetch public share payloads, and duplicate from a share token. Keep owner task-detail methods unchanged and separate the new public-safe contracts from `TaskDetailRecord` so the frontend does not accidentally consume the owner DTO on anonymous pages later.</action>
  <verify>Service tests or component-driven assertions prove the new methods target dedicated share endpoints rather than overloading `getTaskDetail` or generic duplicate</verify>
  <done>The frontend has a clean API layer for both owner share management and later public-share consumption.</done>
</task>

<task type="auto">
  <name>Task 2: Add a share panel to the editor with explicit controls for view and present links</name>
  <files>frontend/src/app/features/library/task-shell-editor-entry.component.ts</files>
  <action>Introduce an owner-facing panel that shows current public share state for the opened saved task and offers clear actions to create, copy, regenerate, and revoke separate `view` and `present` links. Keep the messaging explicit that public present reuses the saved Phase 7 guided-present experience and that sharing publishes only the last saved task state.</action>
  <verify>Component tests or manual checks confirm the panel differentiates the two link modes and never suggests public editing or session tracking</verify>
  <done>Owners can manage both share types directly from the task they are editing.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve the saved-only boundary when share controls are offered</name>
  <files>frontend/src/app/features/library/task-shell-editor-entry.component.ts, frontend/src/app/features/library/task-shell-editor-entry.component.spec.ts</files>
  <action>Disable or warn appropriately when draft media is still pending persistence or a save is in progress, and make it clear that public links reflect only persisted content. Align the copy with the existing preview and guided-present guardrails so Phase 8 does not silently publish draft-only changes or unsaved image selections.</action>
  <verify>`npm test -- --watch=false --browsers=ChromeHeadless` proves pending draft media and active save states gate or message share actions intentionally</verify>
  <done>Sharing respects the same explicit persistence boundary already established by Phases 5 and 7.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test -- --watch=false --browsers=ChromeHeadless` passes in `frontend/`
- [ ] The editor exposes distinct controls for public `view` and public `present` links
- [ ] Share actions are driven by dedicated frontend models and service methods, not by the owner task-detail contract
- [ ] Share UI explains that public links reflect only saved task state
- [ ] Pending draft media or active save state does not silently publish unsaved content
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The authenticated editor can manage share links cleanly while preserving the saved-content boundary

</success_criteria>

<output>
After completion, create `.planning/phases/08-safe-sharing-and-public-access/08-03-SUMMARY.md`
</output>
