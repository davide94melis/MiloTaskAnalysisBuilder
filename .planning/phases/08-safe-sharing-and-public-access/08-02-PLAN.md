---
phase: 08-safe-sharing-and-public-access
plan: 02
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - backend/src/main/java/com/milo/taskbuilder/auth/SecurityConfig.java
  - backend/src/main/java/com/milo/taskbuilder/task/PublicTaskShareController.java
  - backend/src/main/java/com/milo/taskbuilder/task/PublicTaskShareService.java
  - backend/src/main/java/com/milo/taskbuilder/task/PublicTaskDetailMapper.java
  - backend/src/main/java/com/milo/taskbuilder/task/TaskMediaStorageService.java
  - backend/src/main/java/com/milo/taskbuilder/task/TaskShellService.java
  - backend/src/main/java/com/milo/taskbuilder/task/dto/PublicSharedTaskResponse.java
  - backend/src/main/java/com/milo/taskbuilder/task/dto/PublicSharedPresentResponse.java
  - backend/src/test/java/com/milo/taskbuilder/task/PublicTaskShareControllerIntegrationTest.java
autonomous: true
requirements:
  - SHAR-01
  - SHAR-02
  - SHAR-03
  - SHAR-04
  - SHAR-05
  - MEDI-03
user_setup: []
must_haves:
  truths:
    - "Anonymous access must use separate safe DTOs and must never expose the full owner `TaskDetailResponse` payload."
    - "Public media access must be share-token scoped and validated against the currently attached media for that shared task."
    - "Duplication from a shared task remains an authenticated action even if the read surface is anonymous."
    - "Public DTOs must use an explicit allowlist. They exclude `professionalNotes`, `authorName`, `visibility`, `status`, `targetLabel`, lineage or family metadata, and any editor-only navigation hints."
    - "For v1 public sharing, `supportGuidance`, `reinforcementNotes`, and `estimatedMinutes` are excluded from anonymous payloads unless a later plan defines a separate facilitator-safe public contract explicitly."
  artifacts:
    - path: "backend/src/main/java/com/milo/taskbuilder/task/dto/PublicSharedTaskResponse.java"
      provides: "Public-safe task view contract with an explicit field allowlist"
      min_lines: 80
    - path: "backend/src/main/java/com/milo/taskbuilder/task/PublicTaskShareService.java"
      provides: "Public token resolution, DTO assembly, share-scoped media authorization, and duplicate-from-share rules"
      min_lines: 220
    - path: "backend/src/test/java/com/milo/taskbuilder/task/PublicTaskShareControllerIntegrationTest.java"
      provides: "Anonymous/public authorization, DTO minimization, media scoping, revocation, and duplicate-from-share coverage"
      min_lines: 220
  key_links:
    - from: "backend/src/main/java/com/milo/taskbuilder/auth/SecurityConfig.java"
      to: "backend/src/main/java/com/milo/taskbuilder/task/PublicTaskShareController.java"
      via: "security allows only the narrow public share routes instead of opening `/api/**` broadly"
      pattern: "/api/public/shares"
    - from: "backend/src/main/java/com/milo/taskbuilder/task/PublicTaskDetailMapper.java"
      to: "backend/src/main/java/com/milo/taskbuilder/task/dto/TaskDetailResponse.java"
      via: "public mapping must intentionally diverge from the owner payload and omit private editor fields"
      pattern: "professionalNotes"
    - from: "backend/src/main/java/com/milo/taskbuilder/task/PublicTaskShareService.java"
      to: "backend/src/main/java/com/milo/taskbuilder/task/TaskMediaStorageService.java"
      via: "public media bytes are served only after share-token and current-step attachment validation succeeds"
      pattern: "content"
---

<objective>
Expose the backend public-sharing contract safely by adding narrow anonymous routes, explicit public DTOs, share-scoped media access, and an authenticated duplicate-from-share entry point.

Purpose: Phase 8 fails if it simply drops auth from the current owner endpoints. This plan creates a separate public contract that reuses saved task data while keeping editor-only fields, unrelated assets, and generic duplication rules protected.
Output: Anonymous `view` and `present` read endpoints keyed by share token, a share-scoped media endpoint, and an authenticated duplicate-from-share backend flow built on the existing copy semantics.
</objective>

<execution_context>
@C:/Users/davmelis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/davmelis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-safe-sharing-and-public-access/08-RESEARCH.md
@backend/src/main/java/com/milo/taskbuilder/auth/SecurityConfig.java
@backend/src/main/java/com/milo/taskbuilder/library/TaskLibraryController.java
@backend/src/main/java/com/milo/taskbuilder/task/TaskDetailService.java
@backend/src/main/java/com/milo/taskbuilder/task/TaskMediaStorageService.java
@backend/src/main/java/com/milo/taskbuilder/task/TaskShellService.java
@backend/src/main/java/com/milo/taskbuilder/task/dto/TaskDetailResponse.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Carve out narrow public-share routes without weakening the rest of `/api/**`</name>
  <files>backend/src/main/java/com/milo/taskbuilder/auth/SecurityConfig.java, backend/src/main/java/com/milo/taskbuilder/task/PublicTaskShareController.java</files>
  <action>Update security so only the intended public share GET routes are anonymous, while owner task APIs remain authenticated. Keep failures non-leaky for invalid or revoked tokens, and avoid introducing a generic anonymous task or media namespace beyond the share-specific controller.</action>
  <verify>Backend tests prove anonymous access works only on the intended share endpoints and all existing owner task endpoints remain protected</verify>
  <done>The backend exposes a narrow anonymous entry path instead of loosening the whole API surface.</done>
</task>

<task type="auto">
  <name>Task 2: Define public-safe view and present DTOs with explicit field minimization</name>
  <files>backend/src/main/java/com/milo/taskbuilder/task/PublicTaskDetailMapper.java, backend/src/main/java/com/milo/taskbuilder/task/dto/PublicSharedTaskResponse.java, backend/src/main/java/com/milo/taskbuilder/task/dto/PublicSharedPresentResponse.java, backend/src/main/java/com/milo/taskbuilder/task/PublicTaskShareService.java</files>
  <action>Create dedicated public response types and mapping logic that expose only an explicit allowlist of share-safe fields. For `PublicSharedTaskResponse`, limit the contract to task title, description, category if intentionally shown, ordered public steps, and share-scoped media URLs needed for read-only rendering. For `PublicSharedPresentResponse`, limit the contract further to the fields needed by the guided present experience: task title, ordered steps, step title, step description, required flag if kept, and persisted visual-support content. Do not reuse `TaskDetailResponse`, and explicitly omit `professionalNotes`, `authorName`, `visibility`, `status`, `targetLabel`, lineage or family metadata, and owner-only fields. Exclude `supportGuidance`, `reinforcementNotes`, and `estimatedMinutes` from anonymous v1 payloads unless a separate facilitator-safe contract is intentionally introduced and tested.</action>
  <verify>Controller tests assert that excluded owner fields are absent from public JSON, that anonymous payloads contain only the locked allowlist, and that both `view` and `present` payloads resolve from the current saved task state</verify>
  <done>Anonymous consumers receive a purpose-built payload that is safe to render publicly.</done>
</task>

<task type="auto">
  <name>Task 3: Serve public media only through share-scoped authorization tied to current persisted step attachments</name>
  <files>backend/src/main/java/com/milo/taskbuilder/task/PublicTaskShareController.java, backend/src/main/java/com/milo/taskbuilder/task/PublicTaskShareService.java, backend/src/main/java/com/milo/taskbuilder/task/TaskMediaStorageService.java</files>
  <action>Add a public media route such as `/api/public/shares/{token}/media/{mediaId}/content` that validates the share token, confirms the media belongs to the shared task, and confirms it is currently attached to one of the task's persisted steps before reading bytes from storage. Do not expose storage keys directly as globally public URLs, and ensure revocation immediately cuts off media access.</action>
  <verify>`mvn test` covers active share access, invalid token failures, revoked share failures, and rejection for media not attached to the shared task</verify>
  <done>Phase 8 meets `MEDI-03` without creating a generic public asset bucket.</done>
</task>

<task type="auto">
  <name>Task 4: Add an authenticated duplicate-from-share backend path that reuses existing copy semantics</name>
  <files>backend/src/main/java/com/milo/taskbuilder/task/PublicTaskShareController.java, backend/src/main/java/com/milo/taskbuilder/task/PublicTaskShareService.java, backend/src/main/java/com/milo/taskbuilder/task/TaskShellService.java</files>
  <action>Add a dedicated duplicate-from-share endpoint keyed by share token and require authentication for it even when the read routes are anonymous. Resolve the active share safely, then call into the existing duplication mechanics so steps, symbols, and media metadata copy exactly as they do for existing duplicate and variant flows. The resulting copy must always be a new private draft owned by the authenticated recipient.</action>
  <verify>Backend tests prove anonymous duplication is rejected, authenticated recipients can duplicate from an active share, and the new draft remains private and detached from the original share token</verify>
  <done>The backend supports explicit authenticated import from a public share without weakening generic duplicate behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `mvn test` passes in `backend/`
- [ ] Anonymous access works only on the intended share read and share media routes
- [ ] Public DTOs exclude owner-only fields from `TaskDetailResponse`
- [ ] Public media is accessible only through share-token validation and current-step attachment checks
- [ ] Duplicate-from-share requires authentication and creates a new private recipient-owned draft
- [ ] No persisted session tracking or generic public asset URL is introduced
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The backend public-sharing contract is safe, token-scoped, and ready for frontend consumption

</success_criteria>

<output>
After completion, create `.planning/phases/08-safe-sharing-and-public-access/08-02-SUMMARY.md`
</output>
