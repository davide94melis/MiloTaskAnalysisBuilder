---
phase: 08-safe-sharing-and-public-access
plan: 05
type: execute
wave: 4
depends_on:
  - "08-02"
  - "08-03"
  - "08-04"
files_modified:
  - backend/src/test/java/com/milo/taskbuilder/task/PublicTaskShareFlowIntegrationTest.java
  - frontend/src/app/core/tasks/task-library.service.spec.ts
  - frontend/src/app/features/present/task-shared-entry-flow.spec.ts
  - README.md
  - frontend/README-deploy.md
  - backend/README-deploy.md
autonomous: true
requirements:
  - SHAR-01
  - SHAR-02
  - SHAR-03
  - SHAR-04
  - SHAR-05
  - MEDI-03
user_setup: []
must_haves:
  truths:
    - "Phase 8 is not complete without cross-stack verification for anonymous read, share-scoped media, revocation, and authenticated duplication."
    - "Docs must state the public-sharing boundary explicitly: safe DTOs, share-token media, and no persisted sessions yet."
    - "Final verification must leave Phase 9 session tracking and later distribution features out of scope."
  artifacts:
    - path: "backend/src/test/java/com/milo/taskbuilder/task/PublicTaskShareFlowIntegrationTest.java"
      provides: "Cross-endpoint backend regression coverage for active, revoked, invalid, and duplicate-from-share flows"
      min_lines: 180
    - path: "frontend/src/app/features/present/task-shared-entry-flow.spec.ts"
      provides: "Frontend regression coverage for anonymous route load, duplicate auth handoff, and shared present entry behavior"
      min_lines: 180
    - path: "README.md"
      provides: "Updated product-level contract describing public sharing, public present reuse, and explicit duplication boundaries"
      min_lines: 80
  key_links:
    - from: "backend/src/test/java/com/milo/taskbuilder/task/PublicTaskShareFlowIntegrationTest.java"
      to: "frontend/src/app/features/present/task-shared-entry-flow.spec.ts"
      via: "verification closes only when backend authorization and frontend entry flows cover the same public-share matrix"
      pattern: "share"
    - from: "README.md"
      to: "frontend/README-deploy.md"
      via: "product contract and deployment notes must describe the same public route and auth-handoff boundaries"
      pattern: "public"
---

<objective>
Close Phase 8 with regression coverage, documentation, and verification evidence for safe public sharing across backend and frontend boundaries.

Purpose: The implementation work is not enough on its own because this phase introduces a new anonymous surface. The repo needs tests that prove private data stays private, old links stop working when revoked, media stays share-scoped, and duplication remains explicit and authenticated.
Output: Final docs plus regression coverage and verification steps that make Phase 8 checker-friendly even without a separate validation file.
</objective>

<execution_context>
@C:/Users/davmelis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/davmelis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-safe-sharing-and-public-access/08-RESEARCH.md
@README.md
@frontend/README-deploy.md
@backend/README-deploy.md
@frontend/src/app/core/tasks/task-library.service.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cross-stack regression coverage for the public-share matrix</name>
  <files>backend/src/test/java/com/milo/taskbuilder/task/PublicTaskShareFlowIntegrationTest.java, frontend/src/app/core/tasks/task-library.service.spec.ts, frontend/src/app/features/present/task-shared-entry-flow.spec.ts</files>
  <action>Add focused tests that cover active versus revoked versus invalid share tokens, anonymous read access, share-scoped image delivery, and the authenticated duplicate-from-share path. Use at least one mixed-support task fixture so text, symbol, and image content are all exercised under the public contract.</action>
  <verify>`mvn test` and `npm test -- --watch=false --browsers=ChromeHeadless` pass with explicit coverage for revocation, media scoping, and auth-gated duplication</verify>
  <done>The phase has automated proof that the anonymous surface behaves safely across the main risk axes.</done>
</task>

<task type="auto">
  <name>Task 2: Update docs to reflect the exact Phase 8 boundary</name>
  <files>README.md, frontend/README-deploy.md, backend/README-deploy.md</files>
  <action>Document the new owner share-management flow, public view and public present routes, share-scoped public media behavior, and explicit authenticated duplication from shared tasks. State clearly that the public contract uses separate safe DTOs, that existing owner task detail remains authenticated, and that persisted session tracking still belongs to Phase 9.</action>
  <verify>Manual doc review confirms product, frontend, and backend notes describe the same public-share routes and deferrals without contradiction</verify>
  <done>The repo explains the new public-sharing contract accurately and preserves roadmap boundaries for future phases.</done>
</task>

<task type="auto">
  <name>Task 3: Run full Phase 8 verification and capture closure evidence</name>
  <files>README.md, frontend/README-deploy.md, backend/README-deploy.md</files>
  <action>Run the agreed verification commands and perform the manual checks from research: anonymous open for both link types, revoked-link failure, revoked-media failure, safe field exposure, saved-state-only rendering after edits, and authenticated duplication into a private draft. Record enough result detail in the summary to make the phase checker-friendly even though `08-VALIDATION.md` does not exist.</action>
  <verify>`mvn test`, `npm test -- --watch=false --browsers=ChromeHeadless`, and `npm run build` all pass, and the manual checklist is captured in the final summary</verify>
  <done>Phase 8 closes with defensible verification evidence instead of a claim based only on implementation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `mvn test` passes in `backend/`
- [ ] `npm test -- --watch=false --browsers=ChromeHeadless` passes in `frontend/`
- [ ] `npm run build` passes in `frontend/`
- [ ] Automated coverage includes anonymous read, share-scoped media, revocation, and authenticated duplicate-from-share
- [ ] Docs explicitly distinguish safe public DTOs from the authenticated owner task-detail contract
- [ ] Verification notes confirm Phase 8 still excludes persisted session tracking and generic public asset URLs
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 8 closes with documented boundaries and regression evidence strong enough for execution and review

</success_criteria>

<output>
After completion, create `.planning/phases/08-safe-sharing-and-public-access/08-05-SUMMARY.md`
</output>
