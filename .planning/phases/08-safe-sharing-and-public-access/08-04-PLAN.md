---
phase: 08-safe-sharing-and-public-access
plan: 04
type: execute
wave: 3
depends_on:
  - "08-02"
  - "08-03"
files_modified:
  - frontend/src/app/app.routes.ts
  - frontend/src/app/core/auth/milo-auth.service.ts
  - frontend/src/app/features/auth/login-bridge.component.ts
  - frontend/src/app/features/present/task-guided-present-page.component.ts
  - frontend/src/app/features/present/task-guided-present-page.component.spec.ts
  - frontend/src/app/features/present/task-shared-view-page.component.ts
  - frontend/src/app/features/present/task-shared-view-page.component.spec.ts
autonomous: true
requirements:
  - SHAR-01
  - SHAR-02
  - SHAR-03
  - SHAR-04
  - SHAR-05
  - MEDI-03
user_setup: []
must_haves:
  truths:
    - "Public interactive sharing must reuse the Phase 7 guided present experience rather than introducing a second session UI."
    - "Public routes must live outside the authenticated shell, but duplicate-from-share remains an explicit authenticated action."
    - "Anonymous pages must stay public-safe in copy, navigation, and metadata and must not expose editor or family-management affordances."
    - "Public present must suppress editor links, adult-only metadata, family-management controls, and owner-only chrome unless a public DTO explicitly includes a safe equivalent and tests lock it."
  artifacts:
    - path: "frontend/src/app/app.routes.ts"
      provides: "Unauthenticated share routes for public view and public present alongside the existing authenticated task shell"
      min_lines: 35
    - path: "frontend/src/app/features/present/task-guided-present-page.component.ts"
      provides: "Reused guided-present session surface that can load either owner-authenticated or public-safe shared data without persisting sessions"
      min_lines: 280
    - path: "frontend/src/app/features/present/task-shared-view-page.component.ts"
      provides: "Read-only public task page with explicit duplicate action and no editor-only chrome"
      min_lines: 220
  key_links:
    - from: "frontend/src/app/app.routes.ts"
      to: "frontend/src/app/features/present/task-shared-view-page.component.ts"
      via: "the app exposes a public view route outside the auth guard"
      pattern: "shared"
    - from: "frontend/src/app/features/present/task-guided-present-page.component.ts"
      to: "frontend/src/app/core/tasks/task-library.service.ts"
      via: "public interactive links load a share-safe payload rather than owner task detail"
      pattern: "share"
    - from: "frontend/src/app/features/present/task-shared-view-page.component.ts"
      to: "frontend/src/app/core/auth/milo-auth.service.ts"
      via: "duplicate-from-share triggers explicit auth handoff when the recipient is not yet signed in"
      pattern: "login"
---

<objective>
Deliver the public frontend surfaces for shared tasks by adding anonymous routes, reusing the guided present UI safely, and wiring the explicit authenticated duplicate-from-share flow.

Purpose: Phase 8 should feel like an extension of the Phase 7 playback work, not a second parallel player. This plan puts public pages outside the auth shell, keeps the present-mode session logic local-only, and adds the recipient import action without leaking editor controls or draft state.
Output: `/shared/:token` and `/shared/:token/present` routes, a public read-only task page, a share-safe present-mode loader built on the existing guided session surface, and a login return flow for explicit duplication.
</objective>

<execution_context>
@C:/Users/davmelis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/davmelis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-safe-sharing-and-public-access/08-RESEARCH.md
@frontend/src/app/app.routes.ts
@frontend/src/app/features/present/task-guided-present-page.component.ts
@frontend/src/app/features/library/task-shell-editor-entry.component.ts
@frontend/src/app/core/auth/milo-auth.service.ts
@frontend/src/app/features/auth/login-bridge.component.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register public share routes outside the authenticated shell</name>
  <files>frontend/src/app/app.routes.ts, frontend/src/app/features/present/task-shared-view-page.component.ts</files>
  <action>Add unauthenticated routes for a public read-only page and a public interactive present page, keeping the existing `/tasks/:taskId` routes under `authGuard`. Use token-based URL params rather than task ids, and keep the wildcard and login flows working after the new route tree is introduced.</action>
  <verify>Route tests confirm shared routes resolve without the auth shell while authenticated task routes stay guarded</verify>
  <done>The app can open public share links anonymously without weakening the authenticated editor and library routes.</done>
</task>

<task type="auto">
  <name>Task 2: Reuse the guided present session UI for public interactive links</name>
  <files>frontend/src/app/features/present/task-guided-present-page.component.ts, frontend/src/app/features/present/task-guided-present-page.component.spec.ts</files>
  <action>Refactor or extend the existing guided-present page so it can load a public-safe shared payload by token while preserving the Phase 7 local-only session mechanics, responsive layout, and completion behavior. Do not rebuild the session surface from scratch and do not add persisted session tracking, analytics, owner-only navigation, editor links, family controls, or adult-only metadata unless the public DTO explicitly includes a safe equivalent.</action>
  <verify>`npm test -- --watch=false --browsers=ChromeHeadless` covers public-token loading, local step progression, and the absence of owner-only editor links, family controls, and excluded adult-only metadata on the shared present route</verify>
  <done>Public interactive links reuse the proven guided present experience without crossing into Phase 9 tracking work.</done>
</task>

<task type="auto">
  <name>Task 3: Build the public read-only task page with explicit duplication affordances</name>
  <files>frontend/src/app/features/present/task-shared-view-page.component.ts, frontend/src/app/features/present/task-shared-view-page.component.spec.ts</files>
  <action>Create a public-safe task page that renders the shared DTO intentionally, links into the public present route when appropriate, and offers an explicit `duplicate into my library` action. Keep the page free of editor metadata, family navigation, professional notes, and draft-management controls, and ensure image rendering uses the share-scoped public media URLs returned by the backend.</action>
  <verify>Component tests prove the public page renders only the safe DTO fields and that media URLs come from the share-scoped contract instead of owner task URLs</verify>
  <done>Shared tasks are usable without auth for viewing while remaining clearly separate from the private editor surface.</done>
</task>

<task type="auto">
  <name>Task 4: Preserve the share token and intended duplicate action through Milo login</name>
  <files>frontend/src/app/core/auth/milo-auth.service.ts, frontend/src/app/features/auth/login-bridge.component.ts, frontend/src/app/features/present/task-shared-view-page.component.ts</files>
  <action>Wire an explicit auth handoff so an unauthenticated recipient who chooses duplicate is sent into Milo and returned safely with the share token and intended action preserved. After authentication succeeds, call the dedicated duplicate-from-share endpoint, open the new private draft, and avoid auto-duplicating unless the user explicitly requested it.</action>
  <verify>Automated tests or controlled manual flow confirm anonymous viewing remains anonymous, while duplication requires sign-in and returns the recipient to the imported draft after auth</verify>
  <done>Phase 8 supports authenticated import from a public share without blurring the anonymous read boundary.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test -- --watch=false --browsers=ChromeHeadless` passes in `frontend/`
- [ ] Public share routes resolve outside the authenticated shell
- [ ] Public interactive sharing reuses the Phase 7 guided present UI and keeps session progress local-only
- [ ] Public pages do not expose editor-only fields, family management, or owner navigation
- [ ] Duplicate-from-share is explicit, authenticated, and returns the recipient to a private draft
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Public routes, public present reuse, and explicit duplicate-from-share behavior work together without leaking private UI or data

</success_criteria>

<output>
After completion, create `.planning/phases/08-safe-sharing-and-public-access/08-04-SUMMARY.md`
</output>
