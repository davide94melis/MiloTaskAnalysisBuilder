---
phase: 09-minimal-session-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/resources/db/migration/V8__create_task_session_tables.sql
  - backend/src/main/java/com/milo/taskbuilder/task/TaskSessionEntity.java
  - backend/src/main/java/com/milo/taskbuilder/task/TaskSessionRepository.java
  - backend/src/main/java/com/milo/taskbuilder/task/TaskSessionService.java
  - backend/src/main/java/com/milo/taskbuilder/task/TaskSessionController.java
  - backend/src/main/java/com/milo/taskbuilder/task/PublicTaskShareController.java
  - backend/src/main/java/com/milo/taskbuilder/task/dto/CreateTaskSessionRequest.java
  - backend/src/main/java/com/milo/taskbuilder/task/dto/TaskSessionSummaryResponse.java
  - backend/src/test/java/com/milo/taskbuilder/task/TaskSessionControllerIntegrationTest.java
autonomous: true
requirements:
  - SESS-01
  - SESS-02
  - SESS-03
user_setup: []
must_haves:
  truths:
    - "Phase 9 must persist exactly one flat session record per completed run, not per-step telemetry."
    - "Shared present completions must be attributable to the task owner through the active share record while keeping public callers unable to read history."
    - "Owner and public session creation need separate authorization entry points, but both must land in the same narrow session domain."
  artifacts:
    - path: "backend/src/main/resources/db/migration/V8__create_task_session_tables.sql"
      provides: "Persistent minimal session foundation keyed by task, owner, optional share context, completion timestamp, and completed flag"
      min_lines: 35
    - path: "backend/src/main/java/com/milo/taskbuilder/task/TaskSessionService.java"
      provides: "Shared backend rules for owner-session creation, public-share-session creation, and owner-only history lookup"
      min_lines: 220
    - path: "backend/src/test/java/com/milo/taskbuilder/task/TaskSessionControllerIntegrationTest.java"
      provides: "Regression coverage for owner create/read, shared-present create, revoked or wrong-mode denial, and non-owner history denial"
      min_lines: 220
  key_links:
    - from: "backend/src/main/java/com/milo/taskbuilder/task/TaskSessionController.java"
      to: "backend/src/main/java/com/milo/taskbuilder/task/TaskSessionService.java"
      via: "authenticated owner session routes delegate all persistence and authorization rules to the dedicated session service"
      pattern: "sessions"
    - from: "backend/src/main/java/com/milo/taskbuilder/task/PublicTaskShareController.java"
      to: "backend/src/main/java/com/milo/taskbuilder/task/TaskSessionService.java"
      via: "public shared-present completion writes resolve through the same session domain while preserving Phase 8 share validation"
      pattern: "sessions"
---

<objective>
Create the Phase 9 backend session foundation so completed present-mode runs can persist a minimal record and authenticated owners can query basic history for their tasks.

Purpose: The current repo has guided present and safe sharing but no persistence for completions. Phase 9 needs a narrow session domain that supports both owner-present and shared-present writes without dragging in advanced analytics or per-step tracking.
Output: A migrated session table, dedicated backend service, owner and public completion-write endpoints, and owner-only history read support with regression coverage.
</objective>

<execution_context>
@C:/Users/davmelis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/davmelis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-minimal-session-tracking/09-RESEARCH.md
@.planning/phases/09-minimal-session-tracking/09-VALIDATION.md
@backend/src/main/java/com/milo/taskbuilder/task/PublicTaskShareController.java
@backend/src/main/java/com/milo/taskbuilder/task/PublicTaskShareService.java
@backend/src/main/java/com/milo/taskbuilder/task/TaskShellService.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a dedicated minimal session persistence model</name>
  <files>backend/src/main/resources/db/migration/V8__create_task_session_tables.sql, backend/src/main/java/com/milo/taskbuilder/task/TaskSessionEntity.java, backend/src/main/java/com/milo/taskbuilder/task/TaskSessionRepository.java</files>
  <action>Create a dedicated `task_session` persistence model that stores only the fields justified by `SESS-01` and `SESS-02`: task id, owner id, optional share id or access context, completion timestamp, step count, and completed flag. Add indexes for owner- and task-scoped history queries, and do not introduce per-step child rows or analytics columns.</action>
  <verify>`mvn test` passes and the migration does not alter the Phase 8 task/share schema contracts</verify>
  <done>Phase 9 has a durable, narrow session domain ready for both owner and shared present-mode completions.</done>
</task>

<task type="auto">
  <name>Task 2: Add backend service rules and endpoints for owner and shared completion writes</name>
  <files>backend/src/main/java/com/milo/taskbuilder/task/TaskSessionService.java, backend/src/main/java/com/milo/taskbuilder/task/TaskSessionController.java, backend/src/main/java/com/milo/taskbuilder/task/PublicTaskShareController.java, backend/src/main/java/com/milo/taskbuilder/task/dto/CreateTaskSessionRequest.java, backend/src/main/java/com/milo/taskbuilder/task/dto/TaskSessionSummaryResponse.java</files>
  <action>Add authenticated owner endpoints for creating and listing sessions for a task, plus a public-share completion endpoint scoped to `present` shares only. Owner-present completions should attribute the session to the authenticated task owner, while shared-present completions should resolve the active share and attribute the session to the task owner from that share. Keep history reads authenticated and owner-only.</action>
  <verify>Backend tests prove owner create/read works, public `present` shares can create sessions, public `view` shares cannot, and revoked or invalid shares fail safely</verify>
  <done>The backend can now persist minimal sessions across both private and public present flows without leaking history to public callers.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the session boundary with backend regression coverage</name>
  <files>backend/src/test/java/com/milo/taskbuilder/task/TaskSessionControllerIntegrationTest.java</files>
  <action>Add controller and service-level integration coverage for owner session creation, task history ordering, shared-present creation, wrong-mode denial, revoked-share denial, and non-owner history denial. Include at least one shared-present case so Phase 8 public access and Phase 9 persistence are proven to connect correctly.</action>
  <verify>`mvn test` passes with behavioral protection for owner-only history and public-share completion rules</verify>
  <done>The repo has automated proof that Phase 9 backend session persistence stays minimal, safe, and share-aware.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `mvn test` passes in `backend/`
- [ ] A dedicated session table exists with only minimal completion fields
- [ ] Authenticated owners can create and read sessions for their own tasks
- [ ] Public shared-present completions can create sessions, but public view links cannot
- [ ] Revoked or invalid shares cannot create new sessions
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 9 has a narrow backend session foundation that later frontend work can consume safely

</success_criteria>

<output>
After completion, create `.planning/phases/09-minimal-session-tracking/09-01-SUMMARY.md`
</output>
