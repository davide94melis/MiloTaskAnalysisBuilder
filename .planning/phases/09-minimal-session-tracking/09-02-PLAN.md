---
phase: 09-minimal-session-tracking
plan: 02
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - frontend/src/app/core/tasks/task-library.models.ts
  - frontend/src/app/core/tasks/task-library.service.ts
  - frontend/src/app/core/tasks/task-library.service.spec.ts
  - frontend/src/app/features/present/task-guided-present-page.component.ts
  - frontend/src/app/features/present/task-guided-present-page.component.spec.ts
  - frontend/src/app/features/library/task-shell-editor-entry.component.ts
  - frontend/src/app/features/library/task-shell-editor-entry.component.spec.ts
autonomous: true
requirements:
  - SESS-01
  - SESS-02
  - SESS-03
user_setup: []
must_haves:
  truths:
    - "The completed state in guided present must remain immediate and local; session persistence is a follow-up side effect, not a gating transition."
    - "Owner present and shared present must hit different backend write routes but share the same once-per-completed-run guard."
    - "History display should stay minimal and operational, not turn into a dashboard analytics feature."
  artifacts:
    - path: "frontend/src/app/features/present/task-guided-present-page.component.ts"
      provides: "Save-once session persistence from owner and shared present completions with restart-safe guard reset and non-blocking status copy"
      min_lines: 340
    - path: "frontend/src/app/features/library/task-shell-editor-entry.component.ts"
      provides: "Authenticated task-level history surface showing basic recent sessions or counts for the current task"
      min_lines: 950
    - path: "frontend/src/app/features/present/task-guided-present-page.component.spec.ts"
      provides: "Component coverage for owner/shared completion writes, duplicate-write prevention, and restart behavior"
      min_lines: 220
  key_links:
    - from: "frontend/src/app/features/present/task-guided-present-page.component.ts"
      to: "frontend/src/app/core/tasks/task-library.service.ts"
      via: "present mode persists exactly one minimal session record per completed run using the correct owner or shared endpoint"
      pattern: "session"
    - from: "frontend/src/app/features/library/task-shell-editor-entry.component.ts"
      to: "frontend/src/app/core/tasks/task-library.service.ts"
      via: "the authenticated editor loads basic session history for the current task from the new backend session contract"
      pattern: "sessions"
---

<objective>
Add the frontend completion-write and history loop for Phase 9 by persisting minimal sessions from guided present mode and surfacing basic task history for authenticated owners.

Purpose: Backend persistence alone does not satisfy the phase. The user-facing product loop is only complete when present mode saves minimal sessions automatically and owners can see basic history for the current task without entering analytics territory.
Output: Present-mode session save wiring, owner-vs-shared completion handling, simple status feedback, and a minimal authenticated history surface for the current task.
</objective>

<execution_context>
@C:/Users/davmelis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/davmelis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-minimal-session-tracking/09-RESEARCH.md
@.planning/phases/09-minimal-session-tracking/09-VALIDATION.md
@frontend/src/app/features/present/task-guided-present-page.component.ts
@frontend/src/app/features/library/task-shell-editor-entry.component.ts
@frontend/src/app/core/tasks/task-library.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the frontend task service with minimal session create and history APIs</name>
  <files>frontend/src/app/core/tasks/task-library.models.ts, frontend/src/app/core/tasks/task-library.service.ts, frontend/src/app/core/tasks/task-library.service.spec.ts</files>
  <action>Add explicit models and service methods for owner session creation, public shared-present session creation, and authenticated task-level history retrieval. Keep the contract flat and minimal, and lock it with HTTP service tests rather than waiting for component tests to catch wrong URLs or verbs.</action>
  <verify>`npm test -- --watch=false --browsers=ChromeHeadless` covers all new session endpoints and payload shapes</verify>
  <done>The frontend has a stable service-layer contract for Phase 9 session persistence and history.</done>
</task>

<task type="auto">
  <name>Task 2: Persist exactly one session record per completed run from guided present mode</name>
  <files>frontend/src/app/features/present/task-guided-present-page.component.ts, frontend/src/app/features/present/task-guided-present-page.component.spec.ts</files>
  <action>Hook into the existing completed-state transition in guided present mode so owner-present runs call the authenticated task session endpoint and shared-present runs call the public share session endpoint. Add an idempotence guard so revisiting completed steps does not double-write, reset the guard on restart or route change, and keep the completed-task UI immediate even if persistence fails.</action>
  <verify>Component tests prove owner completion writes once, shared completion writes through the public share route, restart enables a second write on a second full completion, and write failures do not block the completed state</verify>
  <done>Phase 9 writes minimal sessions from the real user completion moment without regressing the present-mode UX.</done>
</task>

<task type="auto">
  <name>Task 3: Add a minimal authenticated history surface for the current task</name>
  <files>frontend/src/app/features/library/task-shell-editor-entry.component.ts, frontend/src/app/features/library/task-shell-editor-entry.component.spec.ts</files>
  <action>Add a compact history panel to the authenticated task editor for the current task showing two exact pieces of information: total completion count and the 5 most recent sessions with completed date plus access context. Keep the UI operational and small: no charts, no filters, no analytics language, and one clear empty-state copy when the task has no sessions yet.</action>
  <verify>Frontend tests cover loading history, rendering the empty state, showing total completion count, and listing the 5 most recent sessions with completed date and access context for the opened task only</verify>
  <done>Authenticated owners can now verify that their tasks have been completed without needing a larger reporting feature.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test -- --watch=false --browsers=ChromeHeadless` passes in `frontend/`
- [ ] Guided present completion writes exactly one session per completed run
- [ ] Shared present completion uses the public share session route instead of the owner route
- [ ] Restarting a completed run allows a second session to be created on the next full completion
- [ ] The task editor shows a minimal authenticated history surface or empty state for the current task
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 9 now has the minimal frontend completion/history loop needed for v1

</success_criteria>

<output>
After completion, create `.planning/phases/09-minimal-session-tracking/09-02-SUMMARY.md`
</output>
