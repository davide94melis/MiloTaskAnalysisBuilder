---
phase: 09-minimal-session-tracking
plan: 03
type: execute
wave: 3
depends_on:
  - "09-01"
  - "09-02"
files_modified:
  - backend/src/test/java/com/milo/taskbuilder/task/TaskSessionControllerIntegrationTest.java
  - frontend/src/app/features/present/task-guided-present-page.component.spec.ts
  - frontend/src/app/features/library/task-shell-editor-entry.component.spec.ts
  - README.md
  - frontend/README-deploy.md
  - backend/README-deploy.md
autonomous: true
requirements:
  - SESS-01
  - SESS-02
  - SESS-03
user_setup: []
must_haves:
  truths:
    - "Phase 9 is not complete without proving shared-present, revoked-share, and owner-history boundaries together."
    - "Docs must state clearly that session persistence is minimal completion history, not analytics or clinical tracking."
    - "Final verification must preserve the Phase 8 public-share boundary and keep advanced tracking deferred."
  artifacts:
    - path: "backend/src/test/java/com/milo/taskbuilder/task/TaskSessionControllerIntegrationTest.java"
      provides: "Cross-endpoint regression proof for owner create/read, shared completion, revoked denial, and owner-only history"
      min_lines: 260
    - path: "frontend/src/app/features/present/task-guided-present-page.component.spec.ts"
      provides: "Regression proof for write-once completion semantics and shared-present persistence behavior"
      min_lines: 240
    - path: "README.md"
      provides: "Updated product contract describing minimal session tracking boundaries and deferred advanced analytics"
      min_lines: 90
  key_links:
    - from: "backend/src/test/java/com/milo/taskbuilder/task/TaskSessionControllerIntegrationTest.java"
      to: "frontend/src/app/features/present/task-guided-present-page.component.spec.ts"
      via: "backend authorization and frontend completion behavior must cover the same owner/shared session matrix"
      pattern: "session"
    - from: "README.md"
      to: "frontend/README-deploy.md"
      via: "product and deployment docs must describe the same minimal session-tracking scope and deferrals"
      pattern: "session"
---

<objective>
Close Phase 9 with regression hardening, documentation, and final verification evidence for minimal session tracking.

Purpose: This phase introduces a new write path from present mode and a new owner history surface. It is only complete when the repo proves write-once behavior, share-aware authorization, and the intentionally narrow v1 history boundary.
Output: Hardened tests, updated docs, and final verification evidence strong enough for Phase 9 execution and review.
</objective>

<execution_context>
@C:/Users/davmelis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/davmelis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-minimal-session-tracking/09-RESEARCH.md
@.planning/phases/09-minimal-session-tracking/09-VALIDATION.md
@README.md
@frontend/README-deploy.md
@backend/README-deploy.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden the owner/shared session matrix with regression coverage</name>
  <files>backend/src/test/java/com/milo/taskbuilder/task/TaskSessionControllerIntegrationTest.java, frontend/src/app/features/present/task-guided-present-page.component.spec.ts, frontend/src/app/features/library/task-shell-editor-entry.component.spec.ts</files>
  <action>Add focused regression coverage for owner present session creation, shared present session creation, wrong-mode or revoked share denial, duplicate-write prevention on revisits, second-write allowance after restart, and owner-only history visibility. Use the same behavioral matrix across backend and frontend so the phase closes on aligned evidence instead of isolated happy paths.</action>
  <verify>`mvn test` and `npm test -- --watch=false --browsers=ChromeHeadless` both pass with explicit owner/shared session assertions</verify>
  <done>The repo has automated proof that Phase 9 persistence is minimal, share-aware, and protected against obvious double-write bugs.</done>
</task>

<task type="auto">
  <name>Task 2: Update docs to describe the exact minimal-session boundary</name>
  <files>README.md, frontend/README-deploy.md, backend/README-deploy.md</files>
  <action>Document the new session-create routes, owner-only history access, shared-present attribution behavior, and the intentionally narrow v1 scope. State clearly that Phase 9 stores only minimal completion records and still excludes per-step analytics, timings, prompts used, and clinical tracking.</action>
  <verify>Manual doc review confirms product, frontend, and backend docs all describe the same minimal session boundary without contradicting Phase 8 sharing or later roadmap work</verify>
  <done>The repo communicates the intended v1 tracking scope accurately for future execution and deployment.</done>
</task>

<task type="auto">
  <name>Task 3: Run full Phase 9 verification and capture closure evidence</name>
  <files>README.md, frontend/README-deploy.md, backend/README-deploy.md</files>
  <action>Run the full verification commands and record enough closure evidence to prove owner create/read, shared completion persistence, revoked-share denial, write-once behavior, and authenticated history display. Capture the remaining deferrals explicitly so advanced tracking is not mistaken for Phase 9 scope.</action>
  <verify>`mvn test`, `npm test -- --watch=false --browsers=ChromeHeadless`, and `npm run build` all pass, and the final summary records both automated and manual closure checks</verify>
  <done>Phase 9 closes with defensible evidence instead of a claim based only on implementation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `mvn test` passes in `backend/`
- [ ] `npm test -- --watch=false --browsers=ChromeHeadless` passes in `frontend/`
- [ ] `npm run build` passes in `frontend/`
- [ ] Regression coverage proves owner create/read, shared completion, revoked denial, and write-once behavior
- [ ] Docs explicitly describe minimal completion history and defer advanced tracking
- [ ] Phase 8 public-share boundaries remain intact
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 9 closes with documented minimal-session boundaries and strong regression evidence

</success_criteria>

<output>
After completion, create `.planning/phases/09-minimal-session-tracking/09-03-SUMMARY.md`
</output>
