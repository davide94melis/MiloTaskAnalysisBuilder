---
phase: 07-guided-present-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/app.routes.ts
  - frontend/src/app/features/present/task-guided-present-page.component.ts
  - frontend/src/app/features/present/task-guided-present-page.component.spec.ts
autonomous: true
requirements:
  - PRES-01
  - PRES-02
  - PRES-03
  - PRES-04
  - PRES-05
  - PRES-06
user_setup: []
must_haves:
  truths:
    - "Phase 7 adds a dedicated authenticated present-mode route instead of redefining the Phase 5 preview proof as the final product."
    - "Present mode loads only saved task detail through the existing authenticated task-detail contract and does not depend on editor draft state."
    - "Progress remains local to the browser session in Phase 7 and must not create persisted session records."
  artifacts:
    - path: "frontend/src/app/features/present/task-guided-present-page.component.ts"
      provides: "Dedicated guided present-mode route container with local session state and saved-task loading"
      min_lines: 220
    - path: "frontend/src/app/app.routes.ts"
      provides: "Authenticated route registration for the new present-mode surface"
      min_lines: 30
    - path: "frontend/src/app/features/present/task-guided-present-page.component.spec.ts"
      provides: "Behavior coverage for route load, empty state, and local session-state initialization"
      min_lines: 120
  key_links:
    - from: "frontend/src/app/app.routes.ts"
      to: "frontend/src/app/features/present/task-guided-present-page.component.ts"
      via: "the authenticated shell exposes a dedicated `/tasks/:taskId/present` route for real guided playback"
      pattern: "present"
    - from: "frontend/src/app/features/present/task-guided-present-page.component.ts"
      to: "frontend/src/app/core/tasks/task-library.service.ts"
      via: "present mode fetches the saved task aggregate instead of reading editor-local draft state"
      pattern: "getTaskDetail"
---

<objective>
Establish the Phase 7 present-mode foundation with a dedicated authenticated route, a separate guided-playback component, and a local session model built on top of the already-saved task/media contract.

Purpose: The repo currently has a read-only playback proof. Phase 7 needs a real session-ready surface without rewriting earlier save/media decisions or pulling persistence work forward from Phase 9.
Output: A new authenticated present-mode entry point that loads saved task detail, initializes local navigation/completion state, and handles loading, empty, and error states cleanly.
</objective>

<execution_context>
@C:/Users/davmelis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/davmelis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-guided-present-mode/07-RESEARCH.md
@frontend/src/app/app.routes.ts
@frontend/src/app/features/present/task-playback-preview-page.component.ts
@frontend/src/app/features/present/task-playback-preview-page.component.spec.ts
@frontend/src/app/features/library/task-shell-editor-entry.component.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register a distinct authenticated guided-present route</name>
  <files>frontend/src/app/app.routes.ts, frontend/src/app/features/present/task-guided-present-page.component.ts</files>
  <action>Add a new authenticated route such as `/tasks/:taskId/present` under the existing shell and point it at a dedicated present-mode component. Keep `/tasks/:taskId/preview` intact as the Phase 5 proof surface so tests and docs can continue to distinguish preview from guided present mode.</action>
  <verify>`npm test -- --watch=false --browsers=ChromeHeadless` confirms the route table includes the authenticated guided-present entry without removing preview</verify>
  <done>The app exposes a separate session-ready route for Phase 7 while preserving the preview proof boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Load saved task detail and initialize local session state</name>
  <files>frontend/src/app/features/present/task-guided-present-page.component.ts</files>
  <action>Build the present component around `getTaskDetail(taskId)` and derive ordered saved steps, `currentStepIndex`, `completedStepIndexes` or equivalent, `isFirstStep`, `isLastStep`, `isCurrentStepCompleted`, and `isSessionComplete`. Reset local state when the route changes and do not add backend persistence, timestamps, or history capture.</action>
  <verify>Component tests cover route changes, initial state on saved tasks, one-step tasks, and the absence of persisted-session side effects</verify>
  <done>Guided present mode is stateful enough for navigation and completion without introducing Phase 9 tracking behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Handle loading, error, and zero-step states as first-class outcomes</name>
  <files>frontend/src/app/features/present/task-guided-present-page.component.ts, frontend/src/app/features/present/task-guided-present-page.component.spec.ts</files>
  <action>Define explicit UI states for loading, task-load failure, and tasks with zero saved steps. For zero-step tasks, prefer a calm explanatory empty state with a clear way back to the editor rather than broken controls or a blank page.</action>
  <verify>`npm test -- --watch=false --browsers=ChromeHeadless` proves empty and error states render intentionally and disable guided controls</verify>
  <done>The new route behaves predictably even when saved content is missing or not yet usable for presentation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test -- --watch=false --browsers=ChromeHeadless` passes in `frontend/`
- [ ] `/tasks/:taskId/present` exists under the authenticated shell
- [ ] Present mode loads the saved task aggregate rather than editor-local draft state
- [ ] Loading, error, and zero-step states are intentional and recoverable
- [ ] No public access, share tokens, or persisted session tracking are introduced
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 7 has a dedicated authenticated present-mode foundation that later plans can build on safely

</success_criteria>

<output>
After completion, create `.planning/phases/07-guided-present-mode/07-01-SUMMARY.md`
</output>
