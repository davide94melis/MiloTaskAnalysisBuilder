---
phase: 07-guided-present-mode
plan: 03
type: execute
wave: 3
depends_on:
  - "07-01"
  - "07-02"
files_modified:
  - frontend/src/app/features/present/task-guided-present-page.component.ts
  - frontend/src/app/features/present/task-guided-present-page.component.spec.ts
autonomous: true
requirements:
  - PRES-01
  - PRES-02
  - PRES-03
  - PRES-04
  - PRES-05
  - PRES-06
user_setup: []
must_haves:
  truths:
    - "Step completion in Phase 7 is local UI progress only and must not create backend session or tracking records."
    - "Backward and forward movement stay available during present mode, with one prominent low-friction completion action for the current step."
    - "Finishing the last step must lead to an intentional completion state instead of a disabled final-step dead end."
  artifacts:
    - path: "frontend/src/app/features/present/task-guided-present-page.component.ts"
      provides: "Local navigation, current-step completion, and completed-task flow"
      min_lines: 260
    - path: "frontend/src/app/features/present/task-guided-present-page.component.spec.ts"
      provides: "Coverage for first, middle, last, one-step, and completed-state transitions"
      min_lines: 170
  key_links:
    - from: "frontend/src/app/features/present/task-guided-present-page.component.ts"
      to: "frontend/src/app/features/library/task-shell-editor-entry.component.ts"
      via: "guided present mode stays consistent with the editor's saved-only launch boundary while adding real in-session controls"
      pattern: "preview"
    - from: "frontend/src/app/features/present/task-guided-present-page.component.spec.ts"
      to: ".planning/phases/07-guided-present-mode/07-RESEARCH.md"
      via: "tests cover the edge cases called out in research: zero, one, many, and final completion"
      pattern: "step"
---

<objective>
Implement the guided in-session flow so users can move backward and forward, complete the current step with one clear primary action, and end in a calm task-completed state.

Purpose: A present surface without guided controls is still only a preview. Phase 7 closes the interaction loop locally while staying short of Phase 9 persistence.
Output: Back/next controls, current-step completion behavior, derived session completion, and behavior coverage for first, middle, last, and one-step flows on top of the responsive present surface from `07-02`.
</objective>

<execution_context>
@C:/Users/davmelis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/davmelis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-guided-present-mode/07-RESEARCH.md
@frontend/src/app/features/present/task-guided-present-page.component.ts
@frontend/src/app/features/library/task-shell-editor-entry.component.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add clear back, next, and complete-current-step controls</name>
  <files>frontend/src/app/features/present/task-guided-present-page.component.ts</files>
  <action>Expose predictable navigation controls for previous and next step movement plus one large primary action to mark the current step complete. Keep the primary action prominent and low-friction, while preserving backward navigation so adult facilitators can recover from accidental progress.</action>
  <verify>Component tests cover disabled/enabled behavior at the first and last step boundaries and confirm the primary action remains available for the current step</verify>
  <done>The route supports practical guided playback rather than passive viewing.</done>
</task>

<task type="auto">
  <name>Task 2: Define local completion behavior and final completed-task state</name>
  <files>frontend/src/app/features/present/task-guided-present-page.component.ts, frontend/src/app/features/present/task-guided-present-page.component.spec.ts</files>
  <action>When the current step is completed, update local progress state and either advance to the next step automatically or transition to the final completion state when the last step is completed. The completed view should be explicit, reassuring, and clearly separate from empty/error states.</action>
  <verify>`npm test -- --watch=false --browsers=ChromeHeadless` proves the last-step path reaches a dedicated completed state and that one-step tasks complete cleanly</verify>
  <done>`PRES-04` and `PRES-05` are satisfied without introducing stored sessions or analytics.</done>
</task>

<task type="auto">
  <name>Task 3: Cover session-state edge cases and regressions</name>
  <files>frontend/src/app/features/present/task-guided-present-page.component.spec.ts</files>
  <action>Add behavior-oriented tests for first-step navigation, middle-step navigation, last-step completion, one-step tasks, returning to previously completed steps, and zero-step tasks. Focus on the session model and visible behavior rather than snapshots.</action>
  <verify>Automated tests exercise the core state matrix described in research instead of only a single happy path</verify>
  <done>The present flow is protected against the state regressions most likely to surface during real use.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test -- --watch=false --browsers=ChromeHeadless` passes in `frontend/`
- [ ] Users can move backward and forward between steps during present mode
- [ ] The current step can be completed with one prominent action
- [ ] Completing the final step shows a distinct completed-task state
- [ ] No backend session writes, timestamps, or tracking history are introduced
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Guided present mode includes a complete local session flow from first step to completion

</success_criteria>

<output>
After completion, create `.planning/phases/07-guided-present-mode/07-03-SUMMARY.md`
</output>
