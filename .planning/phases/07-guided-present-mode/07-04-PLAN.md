---
phase: 07-guided-present-mode
plan: 04
type: execute
wave: 4
depends_on:
  - "07-02"
  - "07-03"
files_modified:
  - frontend/src/app/features/library/task-shell-editor-entry.component.ts
  - frontend/src/app/features/library/task-shell-editor-entry.component.spec.ts
  - backend/src/test/java/com/milo/taskbuilder/task/TaskDetailControllerIntegrationTest.java
  - README.md
  - frontend/README-deploy.md
  - backend/README-deploy.md
autonomous: true
requirements:
  - PRES-01
  - PRES-02
  - PRES-03
  - PRES-04
  - PRES-05
  - PRES-06
user_setup: []
must_haves:
  truths:
    - "Present mode launches the currently opened task or variant only; Phase 7 does not add family-switching playback logic."
    - "The editor launch affordance must preserve the saved-only media boundary already established by preview."
    - "Phase 7 closes with docs and verification that explicitly defer public sharing and persisted session tracking to later phases."
  artifacts:
    - path: "frontend/src/app/features/library/task-shell-editor-entry.component.ts"
      provides: "Editor launch affordance for the new guided present route while preserving preview/save messaging"
      min_lines: 320
    - path: "frontend/src/app/features/library/task-shell-editor-entry.component.spec.ts"
      provides: "Coverage for present-route launch, saved-only gating, and current-variant launch behavior"
      min_lines: 170
    - path: "backend/src/test/java/com/milo/taskbuilder/task/TaskDetailControllerIntegrationTest.java"
      provides: "Regression protection that the existing task-detail contract continues to satisfy present-mode needs without a new endpoint"
      min_lines: 120
    - path: "README.md"
      provides: "Documented boundary between authenticated present mode, preview proof, public sharing, and later session tracking"
      min_lines: 40
  key_links:
    - from: "frontend/src/app/features/library/task-shell-editor-entry.component.ts"
      to: "frontend/src/app/features/present/task-guided-present-page.component.ts"
      via: "the editor launches guided present mode for the currently opened saved task or variant"
      pattern: "present"
    - from: "backend/src/test/java/com/milo/taskbuilder/task/TaskDetailControllerIntegrationTest.java"
      to: "backend/src/main/java/com/milo/taskbuilder/task/dto/TaskDetailResponse.java"
      via: "backend tests lock the existing detail payload as the authenticated present-mode contract"
      pattern: "steps"
---

<objective>
Integrate guided present mode into the editor flow, lock the cross-stack contract with regression coverage, and close the phase with docs and verification.

Purpose: Phase 7 is not complete until users can launch the new flow from the authenticated app, the saved-task/media boundary is preserved in that launch path, and the repo documents what remains deferred to Phases 8 and 9.
Output: Editor launch integration, contract and regression tests, updated docs, and final verification closure for the guided present-mode phase.
</objective>

<execution_context>
@C:/Users/davmelis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/davmelis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-guided-present-mode/07-RESEARCH.md
@README.md
@frontend/README-deploy.md
@backend/README-deploy.md
@frontend/src/app/features/library/task-shell-editor-entry.component.ts
@backend/src/test/java/com/milo/taskbuilder/task/TaskDetailControllerIntegrationTest.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add guided present-mode launch from the editor without weakening the save boundary</name>
  <files>frontend/src/app/features/library/task-shell-editor-entry.component.ts, frontend/src/app/features/library/task-shell-editor-entry.component.spec.ts</files>
  <action>Add a clear action to launch `/tasks/:taskId/present` from the current authenticated editor route. Reuse the saved-only gating already used for preview when draft media is still pending persistence, and make it explicit that present mode shows the currently opened task or variant exactly as last saved.</action>
  <verify>`npm test -- --watch=false --browsers=ChromeHeadless` covers launch gating, route navigation, and launching the current variant without extra family-playback controls</verify>
  <done>Users can reach guided present mode from the task they are editing without leaking unsaved media into playback.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the authenticated task-detail contract as the present-mode backend input</name>
  <files>backend/src/test/java/com/milo/taskbuilder/task/TaskDetailControllerIntegrationTest.java</files>
  <action>Add or update backend integration assertions so the existing task-detail payload remains sufficient for guided present mode, including ordered steps, saved visual support, and variant metadata for the current task. Do not add a second present-mode endpoint unless the implementation proved it necessary.</action>
  <verify>`mvn test` passes with behavior-level protection for the contract consumed by the new present route</verify>
  <done>The backend contract stays explicit and stable while backend scope remains intentionally minimal.</done>
</task>

<task type="auto">
  <name>Task 3: Update docs and complete Phase 7 verification closure</name>
  <files>README.md, frontend/README-deploy.md, backend/README-deploy.md</files>
  <action>Document that authenticated guided present mode now exists at the new route, that preview remains a proof-only surface, that public sharing and public media are still Phase 8 work, and that persisted completion/session tracking is still Phase 9 work. Run the full verification commands and complete manual checks for desktop, tablet, phone, one-step, zero-step, and variant launch behavior.</action>
  <verify>`mvn test`, `npm test -- --watch=false --browsers=ChromeHeadless`, and `npm run build` all pass, and docs match the implemented boundaries exactly</verify>
  <done>Phase 7 ends with launch integration, regression protection, and documentation that future phases can build on safely.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `mvn test` passes in `backend/`
- [ ] `npm test -- --watch=false --browsers=ChromeHeadless` passes in `frontend/`
- [ ] `npm run build` passes in `frontend/`
- [ ] The editor launches the guided present route for the currently opened saved task or variant
- [ ] Present launch stays blocked while draft media is pending persistence
- [ ] Docs explicitly preserve the boundary between authenticated present mode, Phase 8 sharing, and Phase 9 session tracking
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 7 closes with integrated launch, protected contracts, and clear documentation of what remains out of scope

</success_criteria>

<output>
After completion, create `.planning/phases/07-guided-present-mode/07-04-SUMMARY.md`
</output>
