---
phase: 07-guided-present-mode
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - frontend/src/app/features/present/task-guided-present-page.component.ts
  - frontend/src/app/features/present/task-guided-present-page.component.spec.ts
autonomous: true
requirements:
  - PRES-01
  - PRES-02
  - PRES-03
  - PRES-04
  - PRES-05
  - PRES-06
user_setup: []
must_haves:
  truths:
    - "Present mode must reduce chrome sharply compared with preview and keep one current step visually dominant."
    - "Responsive usability is a core phase requirement, so phone, tablet, and desktop layout rules must be deliberate rather than incidental stacking."
    - "The existing Phase 5 visual-support contract is reused as-is for text, symbol, image, and mixed-support rendering."
  artifacts:
    - path: "frontend/src/app/features/present/task-guided-present-page.component.ts"
      provides: "Child-facing single-step present surface with responsive layout and saved-media rendering"
      min_lines: 260
    - path: "frontend/src/app/features/present/task-guided-present-page.component.spec.ts"
      provides: "Coverage for single-step rendering, support permutations, and responsive class or layout behavior"
      min_lines: 150
  key_links:
    - from: "frontend/src/app/features/present/task-guided-present-page.component.ts"
      to: "backend/src/main/java/com/milo/taskbuilder/task/dto/TaskDetailResponse.java"
      via: "the UI renders the existing `visualSupport.text`, `visualSupport.symbol`, and `visualSupport.image` shape directly"
      pattern: "visualSupport"
    - from: "frontend/src/app/features/present/task-guided-present-page.component.ts"
      to: "frontend/src/app/features/present/task-playback-preview-page.component.ts"
      via: "Phase 7 can reuse proven helpers and fetch patterns while replacing preview-oriented layout and copy"
      pattern: "show"
---

<objective>
Build the simplified guided present surface so one current step is easy to understand and usable across phone, tablet, and desktop without sacrificing saved media fidelity.

Purpose: The preview proof already confirms the payload works. What is missing is the Phase 7 layout discipline that makes the route usable in real sessions rather than as a verification page.
Output: A mobile-first, child-facing step surface with large visual focus, minimal distractions, and explicit handling of mixed saved visual-support types.
</objective>

<execution_context>
@C:/Users/davmelis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/davmelis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-guided-present-mode/07-RESEARCH.md
@frontend/src/app/features/present/task-playback-preview-page.component.ts
@frontend/src/app/features/present/task-playback-preview-page.component.spec.ts
@backend/src/main/java/com/milo/taskbuilder/task/dto/TaskDetailResponse.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace preview-style framing with a focused present-mode layout</name>
  <files>frontend/src/app/features/present/task-guided-present-page.component.ts</files>
  <action>Design the present-mode template and styles around one current step, large content, and low-distraction chrome. Keep essentials such as task title, step count, current step title, and current step description visible, but avoid dragging preview summary panels, family management UI, or dense metadata blocks into the session surface.</action>
  <verify>Component tests and manual checks confirm only one step is emphasized at a time and the page no longer reads like a verification screen</verify>
  <done>The route feels like a usable guided session surface rather than a playback proof.</done>
</task>

<task type="auto">
  <name>Task 2: Render saved text, symbol, image, and mixed visual supports consistently</name>
  <files>frontend/src/app/features/present/task-guided-present-page.component.ts, frontend/src/app/features/present/task-guided-present-page.component.spec.ts</files>
  <action>Render the persisted visual-support contract exactly as saved, including text-only, symbol-only, image-only, and mixed states. Continue ignoring editor-local upload preview state and keep authenticated image URLs as the only image source used in present mode.</action>
  <verify>`npm test -- --watch=false --browsers=ChromeHeadless` covers all core support permutations and confirms saved media URLs remain the source of truth</verify>
  <done>Present mode preserves the Phase 5 save/media boundary while making saved supports legible in-session.</done>
</task>

<task type="auto">
  <name>Task 3: Make the layout intentionally responsive for phone, tablet, and desktop</name>
  <files>frontend/src/app/features/present/task-guided-present-page.component.ts, frontend/src/app/features/present/task-guided-present-page.component.spec.ts</files>
  <action>Apply mobile-first sizing and spacing so the primary content column, visual-support area, and action region remain usable across narrow phones, portrait tablets, and desktop widths. Use large touch-friendly controls and avoid multi-panel density that forces long scrolling or fine motor precision.</action>
  <verify>Automated tests plus manual viewport checks cover at least one phone-width, tablet-width, and desktop-width presentation outcome</verify>
  <done>The same present route remains readable and touch-friendly across the supported device classes required by `PRES-06`.</done>
</task>

<task type="auto">
  <name>Task 4: Keep professional guidance secondary and optional</name>
  <files>frontend/src/app/features/present/task-guided-present-page.component.ts</files>
  <action>Decide explicitly how support guidance, reinforcement notes, and estimated minutes appear without dominating the child-facing step surface. Favor a secondary or collapsible treatment so adult-assistive details remain available when present but never crowd out the core step content.</action>
  <verify>Manual checks confirm optional metadata appears only when available and stays visually subordinate to the main step content</verify>
  <done>The present surface stays calm and focused even for richly-authored tasks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test -- --watch=false --browsers=ChromeHeadless` passes in `frontend/`
- [ ] Present mode shows one visually dominant current step with minimal distractions
- [ ] Text, symbol, image, and mixed saved supports all render correctly from the persisted contract
- [ ] The layout remains usable on phone, tablet, and desktop widths
- [ ] Family-management and sharing UI do not leak into the live session surface
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Guided present mode has a responsive, child-facing step surface that respects the saved media contract

</success_criteria>

<output>
After completion, create `.planning/phases/07-guided-present-mode/07-02-SUMMARY.md`
</output>
