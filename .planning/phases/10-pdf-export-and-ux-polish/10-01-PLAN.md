---
phase: 10-pdf-export-and-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/app.routes.ts
  - frontend/src/app/features/present/task-print-export-page.component.ts
  - frontend/src/app/features/present/task-print-export-page.component.spec.ts
autonomous: true
requirements:
  - EXPT-01
  - EXPT-02
  - EXPT-03
user_setup: []
must_haves:
  truths:
    - "Phase 10 export must stay frontend-owned and browser-print based for v1; this repo does not need a late backend PDF pipeline."
    - "The export surface must reuse the existing saved task-detail DTO and preserve the same saved-only boundary already used by preview and present."
    - "Printable output must stay structured DOM content with page-aware CSS, not screenshot or canvas capture."
  artifacts:
    - path: "frontend/src/app/features/present/task-print-export-page.component.ts"
      provides: "Dedicated authenticated export route rendering a print-optimized document from saved task detail and triggering browser print safely"
      min_lines: 260
    - path: "frontend/src/app/features/present/task-print-export-page.component.spec.ts"
      provides: "Component coverage for ordered step rendering, mixed visual supports, load/error states, and print-action behavior"
      min_lines: 180
    - path: "frontend/src/app/app.routes.ts"
      provides: "Authenticated `/tasks/:taskId/export` route registration beside preview and present without changing public-share boundaries"
      min_lines: 40
  key_links:
    - from: "frontend/src/app/app.routes.ts"
      to: "frontend/src/app/features/present/task-print-export-page.component.ts"
      via: "the authenticated app shell must expose a dedicated saved-task export route rather than overloading preview or public share routes"
      pattern: "export"
    - from: "frontend/src/app/features/present/task-print-export-page.component.ts"
      to: "frontend/src/app/core/tasks/task-library.service.ts"
      via: "the export page reuses the existing `getTaskDetail` saved-task contract instead of adding a new export DTO or backend API"
      pattern: "getTaskDetail"
---

<objective>
Create the Phase 10 export foundation by adding a dedicated authenticated print/export route that renders a practical browser-print document from the existing saved task-detail contract.

Purpose: Phase 10 needs a credible v1 PDF/export path, but the repo should not absorb a new backend document-generation stack at the end of the milestone. The first wave must prove the route, document model, and print behavior on top of the existing Angular presentation layer.
Output: An authenticated export page, route registration, page-aware print layout for saved task content, and automated coverage for print rendering behavior.
</objective>

<execution_context>
@C:/Users/davmelis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/davmelis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-pdf-export-and-ux-polish/10-RESEARCH.md
@.planning/phases/10-pdf-export-and-ux-polish/10-VALIDATION.md
@frontend/src/app/app.routes.ts
@frontend/src/app/features/present/task-playback-preview-page.component.ts
@frontend/src/app/core/tasks/task-library.service.ts
@frontend/src/app/core/tasks/task-detail.models.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an authenticated print/export route backed by saved task detail</name>
  <files>frontend/src/app/app.routes.ts, frontend/src/app/features/present/task-print-export-page.component.ts</files>
  <action>Add a dedicated authenticated `/tasks/:taskId/export` route under the existing shell and implement a standalone export page component that loads the current saved task through `getTaskDetail`. The page must render a print-focused document with task header, practical metadata, ordered steps, and mixed visual supports while keeping the same saved-only semantics already used by preview and present. Trigger browser print from the page after content loads, but keep an explicit fallback print button visible.</action>
  <verify>`npm test -- --watch=false --browsers=ChromeHeadless` proves the route loads saved task detail, preserves step order, and renders text, symbol, and image support blocks without introducing a new backend dependency</verify>
  <done>Phase 10 has a real export surface that can generate browser PDFs from the existing saved task contract.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the print document model and page behavior with component coverage</name>
  <files>frontend/src/app/features/present/task-print-export-page.component.spec.ts</files>
  <action>Add focused component tests for export rendering and print behavior: ordered step output, empty or missing support combinations, mixed-support tasks, load failures, and the explicit print action. Treat the export page as a structured document surface, not a screenshot feature, and assert that visual-support content stays readable and ordered in DOM form.</action>
  <verify>`npm test -- --watch=false --browsers=ChromeHeadless` covers export route rendering, print CTA behavior, and document fallbacks for missing data</verify>
  <done>The export route is protected against regressions in saved-data loading, layout content composition, and browser-print entry behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test -- --watch=false --browsers=ChromeHeadless` passes in `frontend/`
- [ ] Authenticated `/tasks/:taskId/export` exists under the main app shell
- [ ] The export page reuses `getTaskDetail` instead of introducing a new export API or DTO
- [ ] The print document preserves saved step order and mixed visual supports in structured DOM output
- [ ] Export uses browser print for v1 and does not introduce screenshot-based capture or backend PDF generation
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 10 now has a stable frontend-owned export foundation suitable for practical browser PDF output

</success_criteria>

<output>
After completion, create `.planning/phases/10-pdf-export-and-ux-polish/10-01-SUMMARY.md`
</output>
