---
phase: 06-support-variants
plan: 04
type: execute
wave: 3
depends_on:
  - "06-02"
  - "06-03"
files_modified:
  - backend/src/test/java/com/milo/taskbuilder/task/TaskShellControllerIntegrationTest.java
  - frontend/src/app/features/library/task-shell-editor-entry.component.ts
  - frontend/src/app/features/library/task-shell-editor-entry.component.spec.ts
  - README.md
  - backend/README-deploy.md
  - frontend/README-deploy.md
autonomous: true
requirements:
  - SUPP-01
  - SUPP-02
  - SUPP-03
user_setup: []
must_haves:
  truths:
    - "The editor exposes family context and sibling navigation without implying version diffing or collaboration."
    - "Variant creation and family browsing preserve Phase 5 media fidelity and current explicit save boundaries."
    - "Phase 6 closes with tests and docs that keep present mode and sharing ownership in later phases."
  artifacts:
    - path: "frontend/src/app/features/library/task-shell-editor-entry.component.ts"
      provides: "Editor-side family panel and variant navigation"
      min_lines: 320
    - path: "frontend/src/app/features/library/task-shell-editor-entry.component.spec.ts"
      provides: "Coverage for family panel, sibling navigation, and create-variant handoff"
      min_lines: 120
    - path: "README.md"
      provides: "Documented Phase 6 boundary between support variants, present mode, and sharing"
      min_lines: 30
    - path: "backend/README-deploy.md"
      provides: "Backend contract notes for duplication-based variant families and unchanged media boundary"
      min_lines: 20
    - path: "frontend/README-deploy.md"
      provides: "Frontend contract notes for library/editor family rendering and deferred present/share scope"
      min_lines: 20
    - path: ".planning/phases/06-support-variants/06-04-SUMMARY.md"
      provides: "Recorded manual checklist outcome plus final docs/verification closure for Phase 6"
      min_lines: 40
  key_links:
    - from: "frontend/src/app/features/library/task-shell-editor-entry.component.ts"
      to: "frontend/src/app/core/tasks/task-library.service.ts"
      via: "editor family panel consumes related-variant data and launches family-aware create-variant flow"
      pattern: "variant"
    - from: "README.md"
      to: "backend/src/main/java/com/milo/taskbuilder/task/TaskShellService.java"
      via: "repo docs describe duplication-based variant families as the Phase 6 model"
      pattern: "variant"
---

<objective>
Add editor-side family context and complete the verification/docs loop so support variants feel navigable and stable across the product.

Purpose: Users need to manage variants from the task editor as well as the library, and the phase must close with proof that variant families preserve the existing step/media boundary and stay within v1 scope.
Output: Family panel and sibling navigation on the editor route, cross-stack regression hardening, and docs that record the final Phase 6 boundary.
</objective>

<execution_context>
@C:/Users/davmelis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/davmelis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-support-variants/06-RESEARCH.md
@.planning/phases/06-support-variants/06-VALIDATION.md
@frontend/src/app/features/library/task-shell-editor-entry.component.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a family-aware panel and sibling navigation to the task editor</name>
  <files>frontend/src/app/features/library/task-shell-editor-entry.component.ts, frontend/src/app/features/library/task-shell-editor-entry.component.spec.ts</files>
  <action>Add a compact `Variant family` section to the editor side panel showing whether the current task is standalone, a base task, or a variant, plus links to related variants and an action to create a new variant from the current saved task. Keep this informational and navigational only; do not add diffing, comparison, assignment, or collaboration behavior.</action>
  <verify>`npm test -- --watch=false --browsers=ChromeHeadless` covers sibling rendering, role labeling, and navigation/create-variant handoff from the editor</verify>
  <done>The editor makes support-variant families manageable without turning into a version-management tool.</done>
</task>

<task type="auto">
  <name>Task 2: Harden cross-stack variant-family regressions</name>
  <files>backend/src/test/java/com/milo/taskbuilder/task/TaskShellControllerIntegrationTest.java, frontend/src/app/features/library/task-shell-editor-entry.component.spec.ts</files>
  <action>Add the missing regression coverage around family-aware creation and navigation, including preserved support-level labels, unchanged media duplication assumptions, and correct root/sibling metadata in the task detail payload. Focus on behavior-level assertions, not snapshots.</action>
  <verify>`mvn test` and `npm test -- --watch=false --browsers=ChromeHeadless` both pass with explicit family-aware variant coverage</verify>
  <done>The repo protects the Phase 6 contract against regressions before present mode and sharing phases build on it.</done>
</task>

<task type="auto">
  <name>Task 3: Update docs and capture the final Phase 6 boundary</name>
  <files>README.md, backend/README-deploy.md, frontend/README-deploy.md</files>
  <action>Document that support variants are duplication-based task families distinguished by support level, that Phase 5 media behavior is preserved unchanged inside variants, and that full guided present mode and sharing remain later-phase concerns. Run the full verification commands and complete the manual checklist for family creation, labeling, navigation, and media-preserving variant fidelity.</action>
  <verify>`mvn test`, `npm test -- --watch=false --browsers=ChromeHeadless`, and `npm run build` are all green, and docs accurately describe the implemented support-variant boundary</verify>
  <done>Phase 6 ends with a documented, verified support-variant model that later phases can rely on.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `mvn test` passes in `backend/`
- [ ] `npm test -- --watch=false --browsers=ChromeHeadless` passes in `frontend/`
- [ ] `npm run build` passes in `frontend/`
- [ ] Editor shows family context and sibling navigation without implying version control behavior
- [ ] Variant families preserve existing step/media fidelity
- [ ] Docs explicitly defer present mode and sharing concerns to later phases
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 6 closes with a practical, duplication-based support-variant model visible in both library and editor

</success_criteria>

<output>
After completion, create `.planning/phases/06-support-variants/06-04-SUMMARY.md`
</output>
