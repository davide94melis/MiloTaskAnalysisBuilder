---
phase: 06-support-variants
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - backend/src/main/java/com/milo/taskbuilder/library/dto/CreateTaskRequest.java
  - backend/src/main/java/com/milo/taskbuilder/task/TaskShellController.java
  - backend/src/main/java/com/milo/taskbuilder/task/TaskShellService.java
  - backend/src/test/java/com/milo/taskbuilder/task/TaskShellServiceTest.java
  - backend/src/test/java/com/milo/taskbuilder/task/TaskShellControllerIntegrationTest.java
autonomous: true
requirements:
  - SUPP-01
  - SUPP-02
user_setup: []
must_haves:
  truths:
    - "Variant creation is a specialized duplicate flow, not a separate authoring subsystem."
    - "Creating a variant requires a non-blank support level and preserves the current task/media copy boundary."
    - "Generic duplicate behavior remains available and unchanged."
  artifacts:
    - path: "backend/src/main/java/com/milo/taskbuilder/task/TaskShellService.java"
      provides: "Create-variant service flow built on top of existing copy primitives"
      min_lines: 220
    - path: "backend/src/main/java/com/milo/taskbuilder/task/TaskShellController.java"
      provides: "Variant creation entry point distinct from generic duplicate"
      min_lines: 40
    - path: "backend/src/test/java/com/milo/taskbuilder/task/TaskShellControllerIntegrationTest.java"
      provides: "Contract coverage for create-variant requests and validation"
      min_lines: 70
  key_links:
    - from: "backend/src/main/java/com/milo/taskbuilder/task/TaskShellService.java"
      to: "backend/src/main/java/com/milo/taskbuilder/task/TaskAnalysisStepMediaRepository.java"
      via: "variant creation reuses the existing step/media duplication boundary without cloning files"
      pattern: "copy"
    - from: "backend/src/main/java/com/milo/taskbuilder/library/dto/CreateTaskRequest.java"
      to: "backend/src/main/java/com/milo/taskbuilder/task/TaskShellController.java"
      via: "request shape distinguishes template use, duplicate, and create-variant intents"
      pattern: "support"
---

<objective>
Add the explicit create-variant flow on top of the existing duplication machinery so users can create support-level variants intentionally rather than by generic copy alone.

Purpose: Phase 6 needs a first-class variant action that sets family linkage and support-level metadata correctly while preserving the already-proven step/media copy behavior.
Output: Backend create-variant request/endpoint/service behavior plus tests for family linkage, support-level requirements, and unchanged generic duplicate behavior.
</objective>

<execution_context>
@C:/Users/davmelis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/davmelis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-support-variants/06-RESEARCH.md
@.planning/phases/06-support-variants/06-VALIDATION.md
@backend/src/main/java/com/milo/taskbuilder/task/TaskShellService.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a distinct create-variant request path</name>
  <files>backend/src/main/java/com/milo/taskbuilder/library/dto/CreateTaskRequest.java, backend/src/main/java/com/milo/taskbuilder/task/TaskShellController.java</files>
  <action>Extend the draft-creation API surface so the frontend can explicitly request “create variant” instead of overloading generic duplicate. The request should capture the source task plus required support-level input, with optional title override, while leaving template creation and normal duplicate behavior intact.</action>
  <verify>`mvn test` covers request validation and keeps existing create/duplicate entry points compatible</verify>
  <done>The API exposes a dedicated variant-creation path aligned with the Phase 6 product intent.</done>
</task>

<task type="auto">
  <name>Task 2: Implement family-aware create-variant behavior in the service layer</name>
  <files>backend/src/main/java/com/milo/taskbuilder/task/TaskShellService.java, backend/src/test/java/com/milo/taskbuilder/task/TaskShellServiceTest.java</files>
  <action>Implement `createVariant` on top of the existing copy primitives. Resolve the family root from the source task, require non-blank support level, preserve source/media duplication exactly as Phase 5 implemented it, and keep generic duplicate behavior unchanged for users who just want a copy.</action>
  <verify>`mvn test` proves first-generation and second-generation variants share the same family root and preserve step/media fidelity</verify>
  <done>The backend can create new family members intentionally without introducing a second variant engine.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the create-variant contract with controller tests</name>
  <files>backend/src/test/java/com/milo/taskbuilder/task/TaskShellControllerIntegrationTest.java</files>
  <action>Add integration coverage for variant creation requests, including required support-level validation, returned family metadata, and media-preserving duplication behavior inherited from the existing copy boundary.</action>
  <verify>`mvn test` passes with explicit controller-level coverage for create-variant flows</verify>
  <done>The create-variant backend contract is stable enough for frontend family-aware UI work.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `mvn test` passes in `backend/`
- [ ] Variant creation requires support level and preserves family root correctly
- [ ] Generic duplicate behavior still works unchanged
- [ ] Step and media duplication behavior remains identical to Phase 5
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The backend exposes a reliable, family-aware create-variant flow built on existing duplication primitives

</success_criteria>

<output>
After completion, create `.planning/phases/06-support-variants/06-02-SUMMARY.md`
</output>
