---
phase: 06-support-variants
plan: 03
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - frontend/src/app/core/tasks/task-library.models.ts
  - frontend/src/app/core/tasks/task-library.service.ts
  - frontend/src/app/core/tasks/task-library.service.spec.ts
  - frontend/src/app/features/library/library-page.component.ts
  - frontend/src/app/features/library/library-page.component.spec.ts
  - frontend/src/app/features/library/task-card.component.ts
autonomous: true
requirements:
  - SUPP-02
  - SUPP-03
user_setup: []
must_haves:
  truths:
    - "The library remains card-first while making base and variant tasks visibly distinguishable."
    - "Users can create a variant explicitly without losing the existing generic duplicate action."
    - "Support level is the main visible differentiator for family members."
  artifacts:
    - path: "frontend/src/app/features/library/library-page.component.ts"
      provides: "Family-aware library rendering and create-variant action wiring"
      min_lines: 150
    - path: "frontend/src/app/features/library/task-card.component.ts"
      provides: "Card-level labels/badges for standalone, root, and variant tasks"
      min_lines: 120
    - path: "frontend/src/app/core/tasks/task-library.models.ts"
      provides: "Frontend card models carrying family metadata"
      min_lines: 35
  key_links:
    - from: "frontend/src/app/features/library/library-page.component.ts"
      to: "frontend/src/app/core/tasks/task-library.service.ts"
      via: "family-aware UI consumes the backend variant card contract and create-variant request path"
      pattern: "variant"
    - from: "frontend/src/app/features/library/task-card.component.ts"
      to: "frontend/src/app/core/tasks/task-library.models.ts"
      via: "card badges and labels derive from family role and support-level metadata"
      pattern: "variant"
---

<objective>
Expose support variants clearly in the library and add an explicit create-variant action without turning the library into a management console.

Purpose: Phase 6 is not complete until users can see and distinguish variant families and start a new variant from the same operational library flow they already use.
Output: Family-aware card models, visible base/variant labeling, and a dedicated create-variant action in the library UI.
</objective>

<execution_context>
@C:/Users/davmelis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/davmelis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-support-variants/06-RESEARCH.md
@.planning/phases/06-support-variants/06-VALIDATION.md
@frontend/src/app/features/library/library-page.component.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend frontend library models and service calls for family metadata and variant creation</name>
  <files>frontend/src/app/core/tasks/task-library.models.ts, frontend/src/app/core/tasks/task-library.service.ts, frontend/src/app/core/tasks/task-library.service.spec.ts</files>
  <action>Update the frontend library contract so cards carry family role/root/count metadata and add a dedicated create-variant request path. Keep this aligned with the backend DTO/request shape rather than inventing a frontend-only lineage model.</action>
  <verify>`npm test -- --watch=false --browsers=ChromeHeadless` covers the new family-aware card shape and variant-creation request wiring</verify>
  <done>The frontend can request variant creation and render the backend’s family-aware metadata faithfully.</done>
</task>

<task type="auto">
  <name>Task 2: Add base/variant labeling and explicit create-variant actions to the library</name>
  <files>frontend/src/app/features/library/library-page.component.ts, frontend/src/app/features/library/task-card.component.ts</files>
  <action>Keep the current library card flow but add family-aware badges, support-level emphasis, and a clear `Create variant` action alongside the existing open/duplicate actions. The UI should make it obvious when a task is standalone, a family root, or a variant without introducing nested admin screens.</action>
  <verify>Component tests confirm cards distinguish standalone/root/variant tasks and that create-variant launches the expected backend-backed flow</verify>
  <done>The library makes variant families visible and actionable while staying readable.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve the library’s operational feel while surfacing family grouping cues</name>
  <files>frontend/src/app/features/library/library-page.component.ts, frontend/src/app/features/library/library-page.component.spec.ts</files>
  <action>Render lightweight family context such as sibling counts or grouped cues without converting the page into a nested management tree. Confirm the existing search/filter model still works and the page remains operational and reassuring rather than technical.</action>
  <verify>`npm test -- --watch=false --browsers=ChromeHeadless` proves filtering and card rendering remain stable with mixed standalone and family-linked tasks</verify>
  <done>Phase 6 adds family visibility without regressing the library’s Phase 2 UX foundation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test -- --watch=false --browsers=ChromeHeadless` passes in `frontend/`
- [ ] The library clearly distinguishes standalone, base, and variant tasks
- [ ] `Create variant` is available without removing or overloading generic duplicate
- [ ] Support level is the primary visible differentiator for family members
- [ ] Library filtering and readability remain intact
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The library exposes support variants clearly without becoming a complex management surface

</success_criteria>

<output>
After completion, create `.planning/phases/06-support-variants/06-03-SUMMARY.md`
</output>
