---
phase: 06-support-variants
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/resources/db/migration/V6__add_task_variant_family.sql
  - backend/src/main/java/com/milo/taskbuilder/task/TaskShellEntity.java
  - backend/src/main/java/com/milo/taskbuilder/task/TaskShellRepository.java
  - backend/src/main/java/com/milo/taskbuilder/task/TaskShellService.java
  - backend/src/main/java/com/milo/taskbuilder/task/TaskShellMapper.java
  - backend/src/main/java/com/milo/taskbuilder/library/dto/TaskCardResponse.java
  - backend/src/main/java/com/milo/taskbuilder/task/dto/TaskDetailResponse.java
  - backend/src/test/java/com/milo/taskbuilder/task/TaskShellServiceTest.java
autonomous: true
requirements:
  - SUPP-01
  - SUPP-03
user_setup: []
must_haves:
  truths:
    - "Phase 6 models variants as duplication-based task families rather than a separate versioning engine."
    - "A stable family anchor exists independently from immediate duplication lineage so sibling variants can be grouped reliably."
    - "Support level remains the primary visible differentiator for family members."
  artifacts:
    - path: "backend/src/main/resources/db/migration/V6__add_task_variant_family.sql"
      provides: "Schema support for stable variant family grouping on task shells"
      contains: "variant_family_id"
    - path: "backend/src/main/java/com/milo/taskbuilder/task/TaskShellEntity.java"
      provides: "Task shell lineage model with variant-family anchor"
      min_lines: 90
    - path: "backend/src/main/java/com/milo/taskbuilder/library/dto/TaskCardResponse.java"
      provides: "Card-level family metadata needed for library labeling and grouping"
      min_lines: 20
  key_links:
    - from: "backend/src/main/java/com/milo/taskbuilder/task/TaskShellRepository.java"
      to: "backend/src/main/java/com/milo/taskbuilder/task/TaskShellEntity.java"
      via: "library queries surface family-aware card metadata without changing ownership rules"
      pattern: "variant"
    - from: "backend/src/main/java/com/milo/taskbuilder/task/TaskShellMapper.java"
      to: "backend/src/main/java/com/milo/taskbuilder/library/dto/TaskCardResponse.java"
      via: "family summary fields are populated for frontend library rendering"
      pattern: "variant"
---

<objective>
Add the backend lineage and DTO foundation for variant families so Phase 6 can group and label related support-level variants without building a versioning engine.

Purpose: The repo already duplicates tasks, but it cannot yet distinguish a generic copy from a family of support variants or show sibling relationships reliably.
Output: Variant-family schema support, family-aware card/detail metadata, and repository/mapping changes that make family grouping possible.
</objective>

<execution_context>
@C:/Users/davmelis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/davmelis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-support-variants/06-RESEARCH.md
@.planning/phases/06-support-variants/06-VALIDATION.md
@backend/src/main/java/com/milo/taskbuilder/task/TaskShellEntity.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add variant-family schema support to task shells</name>
  <files>backend/src/main/resources/db/migration/V6__add_task_variant_family.sql, backend/src/main/java/com/milo/taskbuilder/task/TaskShellEntity.java</files>
  <action>Add the minimal lineage field needed for variant grouping, such as nullable `variant_family_id` on `taskbuilder.task_analysis`. Keep `source_task_id` for immediate duplication lineage and do not introduce separate variant tables, version history, workspace rules, or assignment structures.</action>
  <verify>`mvn test` validates the new task-shell entity shape against the schema migration</verify>
  <done>The backend can represent a stable variant family root independently from immediate duplicate lineage.</done>
</task>

<task type="auto">
  <name>Task 2: Extend card/detail contracts with family-aware metadata</name>
  <files>backend/src/main/java/com/milo/taskbuilder/library/dto/TaskCardResponse.java, backend/src/main/java/com/milo/taskbuilder/task/dto/TaskDetailResponse.java, backend/src/main/java/com/milo/taskbuilder/task/TaskShellMapper.java</files>
  <action>Expand task card and task detail contracts so the frontend can tell whether a task is standalone, a base task, or a variant, and can identify the family root and family size. Keep support level as the primary visible label rather than introducing a second variant-label field.</action>
  <verify>`mvn test` proves card/detail payloads include family-aware metadata without breaking existing library/editor consumers</verify>
  <done>The frontend receives enough family context to render grouping and labels without reconstructing lineage heuristically.</done>
</task>

<task type="auto">
  <name>Task 3: Add repository/query support for family-aware library rendering</name>
  <files>backend/src/main/java/com/milo/taskbuilder/task/TaskShellRepository.java, backend/src/test/java/com/milo/taskbuilder/task/TaskShellServiceTest.java</files>
  <action>Update repository/service logic so library queries can return family summary information for standalone tasks, family roots, and variants while keeping the current single-user ownership model intact. Do not change duplicate behavior yet; this plan only establishes the query and metadata foundation.</action>
  <verify>`mvn test` covers mixed standalone and family-linked tasks and keeps existing library behavior stable</verify>
  <done>The backend can support family-aware variant labeling in the library before the create-variant flow lands.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `mvn test` passes in `backend/`
- [ ] Task shells support stable family grouping beyond immediate `source_task_id`
- [ ] Card/detail DTOs expose family metadata without breaking existing contracts
- [ ] No version-history engine, collaboration model, or sharing behavior is introduced
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The backend has the minimum lineage and DTO foundation needed for support variants

</success_criteria>

<output>
After completion, create `.planning/phases/06-support-variants/06-01-SUMMARY.md`
</output>
