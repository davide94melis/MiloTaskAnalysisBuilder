---
phase: 05-media-support-pipeline
plan: 05
type: execute
wave: 5
depends_on:
  - "05-04"
files_modified:
  - frontend/src/app/features/present/task-playback-preview-page.component.spec.ts
  - frontend/src/app/features/library/task-shell-editor-entry.component.spec.ts
  - frontend/src/app/core/tasks/task-library.service.spec.ts
  - README.md
  - .planning/phases/05-media-support-pipeline/05-VERIFICATION.md
autonomous: true
requirements:
  - STEP-03
  - STEP-04
  - MEDI-01
  - MEDI-02
gap_closure: true
user_setup: []
must_haves:
  truths:
    - "Gap closure is only complete when save-to-preview behavior is verified using persisted media, not just route wiring."
    - "Phase 5 documentation must explicitly describe this preview as verification-oriented playback proof and defer full guided present mode to Phase 7."
    - "Regression coverage must protect mixed visual-support fidelity and authenticated media loading in the preview route without claiming public sharing behavior."
  artifacts:
    - path: "frontend/src/app/features/present/task-playback-preview-page.component.spec.ts"
      provides: "Behavior coverage for authenticated read-only media rendering in preview context"
      min_lines: 80
    - path: "README.md"
      provides: "Documented boundary between Phase 5 playback proof, Phase 7 present mode, and Phase 8 public sharing"
      min_lines: 25
    - path: ".planning/phases/05-media-support-pipeline/05-VERIFICATION.md"
      provides: "Updated verification record showing the prior playback-context gap has been closed narrowly"
      min_lines: 40
  key_links:
    - from: "frontend/src/app/features/present/task-playback-preview-page.component.spec.ts"
      to: "frontend/src/app/core/tasks/task-library.service.spec.ts"
      via: "preview tests protect the same authenticated task-detail/media contract used by the service layer"
      pattern: "getTaskDetail"
    - from: "README.md"
      to: ".planning/phases/05-media-support-pipeline/05-VERIFICATION.md"
      via: "docs and verification record align on the exact boundary of the gap closure"
      pattern: "preview"
---

<objective>
Harden and verify the new Phase 5 playback proof so the verification gap closes cleanly without collapsing into Phase 7 implementation.

Purpose: Adding a preview route is not enough by itself. The repo needs explicit tests and documentation showing that saved media now loads outside the editor, while guided present mode, completion flow, and public sharing are still deferred.
Output: Regression coverage, updated verification evidence, and repo documentation that captures the narrow scope of the gap closure.
</objective>

<execution_context>
@C:/Users/davmelis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/davmelis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-media-support-pipeline/05-VERIFICATION.md
@frontend/src/app/features/present/task-playback-preview-page.component.ts
@frontend/src/app/core/tasks/task-library.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add regression tests for save-to-preview media fidelity</name>
  <files>frontend/src/app/features/present/task-playback-preview-page.component.spec.ts, frontend/src/app/features/library/task-shell-editor-entry.component.spec.ts, frontend/src/app/core/tasks/task-library.service.spec.ts</files>
  <action>Add or tighten tests that cover the exact gap-closing path: save a task with mixed visual supports, open the authenticated preview route, and confirm the preview renders the persisted media descriptors returned by the task-detail contract. Include a negative case proving unsaved local upload state is not the source of truth for preview rendering.</action>
  <verify>`npm test -- --watch=false --browsers=ChromeHeadless` passes with explicit coverage for saved media loading in preview context</verify>
  <done>The repo has durable automated proof that media works outside the editor using persisted data.</done>
</task>

<task type="auto">
  <name>Task 2: Document the narrow playback-proof boundary and keep later phase ownership intact</name>
  <files>README.md</files>
  <action>Update the repo documentation so it clearly states that Phase 5 now includes an authenticated read-only preview used to prove media playback fidelity, while full guided present mode remains Phase 7 and public sharing remains Phase 8. Record that the preview reuses the existing authenticated media pipeline and does not alter the save contract.</action>
  <verify>`npm run build` still passes and the README accurately describes the implemented preview boundary and phase ownership</verify>
  <done>The intended scope is documented clearly enough that future work does not reopen this planning boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Update the Phase 5 verification record with the closed-gap evidence</name>
  <files>.planning/phases/05-media-support-pipeline/05-VERIFICATION.md</files>
  <action>Revise the verification write-up so it records the new authenticated preview surface, cites the saved-contract proof points, and marks the original playback-context gap as closed. Keep the wording precise that this is a minimal proof surface, not the full guided present mode promised by Phase 7.</action>
  <verify>The verification document explicitly references the new preview route, the saved media rendering evidence, and the still-deferred Phase 7/8 concerns</verify>
  <done>The phase record aligns with the actual implementation and no longer overstates or understates Phase 5 scope.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test -- --watch=false --browsers=ChromeHeadless` passes in `frontend/`
- [ ] `npm run build` passes in `frontend/`
- [ ] Automated coverage proves persisted mixed-media steps render in preview context outside the editor
- [ ] Documentation explicitly preserves Phase 7 ownership of guided present mode and Phase 8 ownership of public media access
- [ ] `.planning/phases/05-media-support-pipeline/05-VERIFICATION.md` records the gap as closed for the narrow authenticated playback proof
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 5 verification reflects a closed playback-context gap without absorbing full present mode scope

</success_criteria>

<output>
After completion, create `.planning/phases/05-media-support-pipeline/05-05-SUMMARY.md`
</output>
