---
phase: 05-media-support-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - frontend/src/app/core/tasks/task-detail.models.ts
  - frontend/src/app/core/tasks/task-library.service.ts
  - frontend/src/app/core/tasks/task-library.service.spec.ts
  - frontend/src/app/features/library/task-shell-editor-entry.component.ts
  - frontend/src/app/features/library/task-shell-editor-entry.component.spec.ts
  - frontend/src/app/features/library/task-step-authoring-editor.component.ts
  - frontend/src/app/features/library/task-step-authoring-editor.component.spec.ts
  - frontend/src/app/features/library/task-symbol-catalog.ts
autonomous: true
requirements:
  - STEP-03
  - STEP-04
  - MEDI-01
  - MEDI-02
user_setup: []
must_haves:
  truths:
    - "The existing `/tasks/:taskId` editor remains the single authoring surface for step media support."
    - "Each step can carry text-only, symbol-plus-text, image-only, or photo-plus-text support without introducing a generic media manager."
    - "Image upload state is handled locally per step while final persistence still happens through the explicit task save action."
    - "The step editor remains understandable for therapist and teacher workflows even after media controls are added."
  artifacts:
    - path: "frontend/src/app/features/library/task-step-authoring-editor.component.ts"
      provides: "Extended step editor with visual support controls, preview state, and inline upload feedback"
      min_lines: 180
    - path: "frontend/src/app/features/library/task-shell-editor-entry.component.ts"
      provides: "Route-level orchestration for upload, draft state, and explicit task save with mixed visual support payloads"
      min_lines: 140
    - path: "frontend/src/app/core/tasks/task-detail.models.ts"
      provides: "Frontend task detail model carrying nested `visualSupport` and upload descriptor data"
      min_lines: 40
  key_links:
    - from: "frontend/src/app/features/library/task-shell-editor-entry.component.ts"
      to: "frontend/src/app/core/tasks/task-library.service.ts"
      via: "authoring route loads, uploads, and saves the expanded Phase 5 task contract"
      pattern: "updateTask"
    - from: "frontend/src/app/features/library/task-step-authoring-editor.component.ts"
      to: "frontend/src/app/core/tasks/task-detail.models.ts"
      via: "step cards manipulate the nested visual support draft shape instead of ad hoc fields"
      pattern: "visualSupport"
---

<objective>
Extend the Phase 4 step editor into a practical visual-support authoring surface for symbols, photos, and short child-facing text.

Purpose: The product only becomes materially different from a text checklist when media can be attached inside the step workflow without making the editor clumsy or fragile.
Output: Expanded frontend models, upload-aware step authoring UI, symbol selection, local preview/error states, and explicit save wiring on `/tasks/:taskId`.
</objective>

<execution_context>
@C:/Users/davmelis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/davmelis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-media-support-pipeline/05-RESEARCH.md
@.planning/phases/05-media-support-pipeline/05-VALIDATION.md
@frontend/src/app/features/library/task-step-authoring-editor.component.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand frontend task models and service calls for visual support plus upload descriptors</name>
  <files>frontend/src/app/core/tasks/task-detail.models.ts, frontend/src/app/core/tasks/task-library.service.ts, frontend/src/app/core/tasks/task-library.service.spec.ts</files>
  <action>Update the frontend task detail contract so each step carries a nested `visualSupport` object and add a service call for the new task-scoped image upload endpoint. Keep this aligned with the backend DTO shape rather than inventing a frontend-only media model.</action>
  <verify>`npm test -- --watch=false --browsers=ChromeHeadless` covers the new upload call and the expanded task detail payload</verify>
  <done>The frontend can both upload image files and save mixed visual supports through the canonical task detail contract.</done>
</task>

<task type="auto">
  <name>Task 2: Add visual support controls, upload preview, and symbol selection to the step editor</name>
  <files>frontend/src/app/features/library/task-step-authoring-editor.component.ts, frontend/src/app/features/library/task-step-authoring-editor.component.spec.ts, frontend/src/app/features/library/task-symbol-catalog.ts</files>
  <action>Extend each step card with a dedicated visual-support section that lets the user enter short visual text, pick a symbol from a lightweight catalog, upload one image/photo, preview the result, and remove or replace supports. Keep the layout calm and operational rather than turning the route into a media-management screen.</action>
  <verify>Component tests prove text-only, symbol-plus-text, photo-plus-text, and upload-failure behaviors while preserving the existing add/edit/reorder/duplicate/delete step interactions</verify>
  <done>The step editor supports the Phase 5 media combinations directly inside the authoring workflow.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve explicit save semantics and local upload state on the task route</name>
  <files>frontend/src/app/features/library/task-shell-editor-entry.component.ts, frontend/src/app/features/library/task-shell-editor-entry.component.spec.ts</files>
  <action>Keep the route container responsible for task load/save orchestration while handling per-step upload status, unsaved media state, and save notices clearly. Users should be able to upload to a local step draft, continue editing other fields, and then persist the full mixed-media step array with the normal explicit save action. Make the route treat backend-returned media descriptors and post-save reload fidelity as the concrete Phase 5 reliability contract.</action>
  <verify>`npm test -- --watch=false --browsers=ChromeHeadless` proves upload results are retained in draft state, backend-returned media descriptors survive explicit save, and task reloads preserve the same mixed visual-support composition</verify>
  <done>The `/tasks/:taskId` workflow remains predictable even with asynchronous uploads in the middle of authoring and satisfies the Phase 5 media reliability boundary.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test -- --watch=false --browsers=ChromeHeadless` passes in `frontend/`
- [ ] `npm run build` passes in `frontend/`
- [ ] Each step can save at least one visual support and mixed combinations like symbol + text or photo + text
- [ ] Upload failures are scoped to the affected step and do not wipe the rest of the draft
- [ ] The editor remains a single clear authoring workflow with explicit save behavior
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The frontend delivers practical step media authoring without breaking the core task-editing flow

</success_criteria>

<output>
After completion, create `.planning/phases/05-media-support-pipeline/05-02-SUMMARY.md`
</output>
