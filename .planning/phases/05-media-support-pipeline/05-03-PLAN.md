---
phase: 05-media-support-pipeline
plan: 03
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - backend/src/main/java/com/milo/taskbuilder/task/TaskShellService.java
  - backend/src/test/java/com/milo/taskbuilder/task/TaskDetailControllerIntegrationTest.java
  - backend/src/test/java/com/milo/taskbuilder/task/TaskDetailServiceTest.java
  - backend/src/test/java/com/milo/taskbuilder/task/TaskShellServiceTest.java
  - frontend/src/app/core/tasks/task-library.service.spec.ts
  - frontend/src/app/features/library/task-shell-editor-entry.component.spec.ts
  - frontend/src/app/features/library/task-step-authoring-editor.component.spec.ts
  - README.md
  - backend/README-deploy.md
  - frontend/README-deploy.md
autonomous: true
requirements:
  - STEP-03
  - STEP-04
  - MEDI-01
  - MEDI-02
user_setup: []
must_haves:
  truths:
    - "Phase 5 closes with reliable save/reload and duplication behavior for step media before present mode begins."
    - "Documentation records the authenticated media contract and explicitly defers public asset exposure to Phase 8."
    - "Cross-stack verification proves the new media pipeline does not regress the existing step authoring workflow."
  artifacts:
    - path: "backend/src/main/java/com/milo/taskbuilder/task/TaskShellService.java"
      provides: "Task duplication logic that preserves Phase 5 visual support metadata and image references"
      min_lines: 110
    - path: "backend/src/test/java/com/milo/taskbuilder/task/TaskShellServiceTest.java"
      provides: "Regression coverage for duplication fidelity with visual supports"
      min_lines: 60
    - path: "README.md"
      provides: "Documented Phase 5 media boundary and contract for future present/share work"
      min_lines: 25
  key_links:
    - from: "backend/src/main/java/com/milo/taskbuilder/task/TaskShellService.java"
      to: "backend/src/main/java/com/milo/taskbuilder/task/TaskAnalysisStepMediaEntity.java"
      via: "duplicate-task behavior preserves saved media references instead of dropping them"
      pattern: "media"
    - from: "README.md"
      to: "backend/src/main/java/com/milo/taskbuilder/task/TaskMediaController.java"
      via: "repo docs describe the authenticated upload contract and its relation to explicit task save"
      pattern: "upload"
---

<objective>
Harden, verify, and document the Phase 5 media workflow so later present-mode and sharing phases can build on a stable contract.

Purpose: Media support is only useful if reload fidelity, duplication behavior, and documentation are solid enough that Phase 6 and Phase 7 do not need to reopen Phase 5 architecture.
Output: Duplication-safe media handling, cross-stack regression coverage, docs updates, and a fully verified Phase 5 boundary.
</objective>

<execution_context>
@C:/Users/davmelis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/davmelis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-media-support-pipeline/05-RESEARCH.md
@.planning/phases/05-media-support-pipeline/05-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preserve visual supports during task duplication and normal persistence hardening</name>
  <files>backend/src/main/java/com/milo/taskbuilder/task/TaskShellService.java, backend/src/test/java/com/milo/taskbuilder/task/TaskShellServiceTest.java, backend/src/test/java/com/milo/taskbuilder/task/TaskDetailServiceTest.java</files>
  <action>Extend duplicate-task behavior so saved visual text, symbol references, and image metadata remain attached when a task is copied. Keep the v1 rule simple by copying media references and metadata first rather than cloning physical image objects, and cover the save path so normal reorder/edit flows do not orphan media associations.</action>
  <verify>`mvn test` proves duplicated tasks preserve Phase 5 visual supports and that subsequent edits still round-trip correctly</verify>
  <done>Media support behaves predictably across copy workflows that later variant features will depend on.</done>
</task>

<task type="auto">
  <name>Task 2: Close backend/frontend regression gaps for reliability and mixed-modality fidelity</name>
  <files>backend/src/test/java/com/milo/taskbuilder/task/TaskDetailControllerIntegrationTest.java, frontend/src/app/core/tasks/task-library.service.spec.ts, frontend/src/app/features/library/task-shell-editor-entry.component.spec.ts, frontend/src/app/features/library/task-step-authoring-editor.component.spec.ts</files>
  <action>Add or tighten tests around authenticated upload, GET/PUT round-trip fidelity, mixed visual support combinations, upload failure handling, and exact save/reload behavior. Focus on behavior-level assertions that protect the contract Phase 7 present mode will consume.</action>
  <verify>`mvn test` and `npm test -- --watch=false --browsers=ChromeHeadless` both pass with explicit coverage for Phase 5 mixed-modality and reliability behavior</verify>
  <done>The repo has regression coverage for the core Phase 5 promises instead of relying on manual checks alone.</done>
</task>

<task type="auto">
  <name>Task 3: Update docs and run full verification with the Phase 5 boundary captured explicitly</name>
  <files>README.md, backend/README-deploy.md, frontend/README-deploy.md</files>
  <action>Document the new authenticated media upload contract, the nested step `visualSupport` model, the rule that saved image data uses stable storage identifiers instead of persisted signed URLs, and the fact that public asset exposure stays deferred to Phase 8. Run the full verification commands and complete the manual workflow checklist for upload, mixed modalities, save/reload fidelity, and duplication.</action>
  <verify>`mvn test`, `npm test -- --watch=false --browsers=ChromeHeadless`, and `npm run build` are all green, and docs accurately describe the implemented Phase 5 media boundary</verify>
  <done>Phase 5 ends with reliable media behavior, supporting documentation, and no ambiguity about what later phases still own.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `mvn test` passes in `backend/`
- [ ] `npm test -- --watch=false --browsers=ChromeHeadless` passes in `frontend/`
- [ ] `npm run build` passes in `frontend/`
- [ ] Duplication preserves saved visual supports and image references
- [ ] Docs explicitly defer public media exposure to Phase 8 and present-mode UI to Phase 7
- [ ] Manual checklist confirms upload, mixed-modality editing, save/reload, and duplication all stay usable
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 5 ends with a documented, duplication-safe, regression-tested media authoring pipeline ready for later playback and sharing phases

</success_criteria>

<output>
After completion, create `.planning/phases/05-media-support-pipeline/05-03-SUMMARY.md`
</output>
