---
phase: 05-media-support-pipeline
plan: 04
type: execute
wave: 4
depends_on:
  - "05-03"
files_modified:
  - frontend/src/app/app.routes.ts
  - frontend/src/app/features/library/task-shell-editor-entry.component.ts
  - frontend/src/app/features/library/task-shell-editor-entry.component.spec.ts
  - frontend/src/app/features/present/task-playback-preview-page.component.ts
  - frontend/src/app/features/present/task-playback-preview-page.component.spec.ts
autonomous: true
requirements:
  - STEP-03
  - STEP-04
  - MEDI-01
  - MEDI-02
gap_closure: true
user_setup: []
must_haves:
  truths:
    - "Phase 5 closes its verification gap by proving saved media renders in an authenticated read-only playback surface outside the editor."
    - "The new surface reuses the saved task detail contract and existing media delivery path instead of introducing a new save model."
    - "Scope remains intentionally smaller than Phase 7: no session state, no completion flow, no public access, and no full guided present-mode chrome."
    - "Preview behavior must be child-facing and distraction-light enough to count as playback context without absorbing full present-mode requirements."
  artifacts:
    - path: "frontend/src/app/features/present/task-playback-preview-page.component.ts"
      provides: "Minimal authenticated playback/preview screen that renders saved step visual supports in read-only form"
      min_lines: 140
    - path: "frontend/src/app/app.routes.ts"
      provides: "Authenticated route entry for the playback preview surface"
      min_lines: 20
    - path: "frontend/src/app/features/library/task-shell-editor-entry.component.ts"
      provides: "Editor-side launch path into the proof-only playback preview after save"
      min_lines: 250
  key_links:
    - from: "frontend/src/app/app.routes.ts"
      to: "frontend/src/app/features/present/task-playback-preview-page.component.ts"
      via: "authenticated users can open a dedicated playback preview route outside the editor shell"
      pattern: "preview"
    - from: "frontend/src/app/features/library/task-shell-editor-entry.component.ts"
      to: "frontend/src/app/features/present/task-playback-preview-page.component.ts"
      via: "editor actions launch the read-only preview to prove saved media fidelity beyond authoring"
      pattern: "preview"
---

<objective>
Add the smallest authenticated playback surface needed to close the Phase 5 verification gap: a read-only preview that renders saved step media outside the editor and proves the existing media pipeline works in a child-facing context.

Purpose: The verifier already accepts the editor-side media pipeline. The missing proof is that the same saved text, symbol, and image supports load in a non-editor surface without relying on transient draft state.
Output: An authenticated preview route, a read-only playback component, and editor navigation that opens the saved task in that preview context.
</objective>

<execution_context>
@C:/Users/davmelis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/davmelis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-media-support-pipeline/05-VERIFICATION.md
@frontend/src/app/app.routes.ts
@frontend/src/app/features/library/task-shell-editor-entry.component.ts
@frontend/src/app/core/tasks/task-library.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an authenticated route for Phase 5 playback proof</name>
  <files>frontend/src/app/app.routes.ts, frontend/src/app/features/library/task-shell-editor-entry.component.ts</files>
  <action>Create a dedicated authenticated route such as `/tasks/:taskId/preview` that lives outside the edit form experience but inside the existing protected app shell. Add a clear editor action that launches this preview only from saved task data, so users can validate how media appears in playback context without introducing full Phase 7 launch patterns.</action>
  <verify>`npm test -- --watch=false --browsers=ChromeHeadless` covers route registration plus editor navigation into the preview surface</verify>
  <done>The app has a real, authenticated path into a playback-style read-only context for saved tasks.</done>
</task>

<task type="auto">
  <name>Task 2: Build a minimal child-facing read-only preview that consumes saved step media</name>
  <files>frontend/src/app/features/present/task-playback-preview-page.component.ts, frontend/src/app/features/present/task-playback-preview-page.component.spec.ts</files>
  <action>Implement a focused preview screen that reads the saved task detail payload and renders one step at a time in a calm, child-facing layout with large visual support treatment. Show saved visual text, symbols, and images exactly as returned by the existing contract. Allow only the minimum browsing needed to inspect more than one saved step, such as previous/next navigation or step paging, while omitting completion actions, progress tracking, timers, and any guided session state.</action>
  <verify>Component tests prove the preview renders text-only, symbol-plus-text, and photo-plus-text steps from saved task detail data and remains read-only</verify>
  <done>Saved media now renders outside the editor in a playback-style context that is visibly distinct from authoring but still below full present mode.</done>
</task>

<task type="auto">
  <name>Task 3: Ensure the preview uses saved contract data rather than transient editor draft state</name>
  <files>frontend/src/app/features/library/task-shell-editor-entry.component.ts, frontend/src/app/features/library/task-shell-editor-entry.component.spec.ts, frontend/src/app/features/present/task-playback-preview-page.component.spec.ts</files>
  <action>Treat preview launch as a save-aware flow. If needed, route users through the last persisted version or make the preview action clearly depend on saved task data. Add coverage that prevents accidental reuse of local upload draft state, so the proof remains about persisted media reliability rather than about in-memory editor state.</action>
  <verify>`npm test -- --watch=false --browsers=ChromeHeadless` confirms preview reads persisted task detail data and does not silently rely on unsaved draft media</verify>
  <done>The gap closure genuinely proves the Phase 5 save contract in playback context instead of only reusing authoring state.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test -- --watch=false --browsers=ChromeHeadless` passes in `frontend/`
- [ ] `npm run build` passes in `frontend/`
- [ ] An authenticated user can open a dedicated preview route outside the editor
- [ ] The preview renders saved visual text, symbols, and images using the existing `visualSupport` contract
- [ ] The preview is read-only and does not add completion state, session logging, or public access behavior
- [ ] The implementation does not change the existing upload pipeline or task save contract
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 5 has a minimal non-editor playback surface that proves saved media loads in authenticated read-only context

</success_criteria>

<output>
After completion, create `.planning/phases/05-media-support-pipeline/05-04-SUMMARY.md`
</output>
