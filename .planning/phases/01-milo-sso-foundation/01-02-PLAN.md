---
phase: 01-milo-sso-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/angular.json
  - frontend/public/config.json
  - frontend/scripts/write-config.js
  - frontend/src/app/core/config/app-config.service.ts
  - frontend/src/app/core/auth/milo-auth.service.ts
  - frontend/src/app/core/auth/auth.interceptor.ts
  - frontend/src/app/core/auth/auth.guard.ts
  - frontend/src/app/app.routes.ts
  - frontend/src/app/features/auth/login-bridge.component.ts
  - frontend/src/app/layout/main-layout.component.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
user_setup:
  - service: app-config
    why: "The frontend needs the backend base URL and Milo API linkage through build-time config."
    env_vars:
      - name: API_URL
        source: "Task Builder frontend build environment"
      - name: MILO_API_URL
        source: "Milo backend base API URL if separate bridging is needed"
must_haves:
  truths:
    - "The frontend can restore an authenticated Milo-backed session after refresh."
    - "Protected frontend API calls include the bearer token automatically."
    - "A 401 invalid-session response clears auth state and redirects safely."
  artifacts:
    - path: "frontend/src/app/core/auth/milo-auth.service.ts"
      provides: "Token persistence and session bootstrap logic"
      min_lines: 30
    - path: "frontend/src/app/core/auth/auth.interceptor.ts"
      provides: "Automatic Authorization header injection and 401 handling"
      min_lines: 20
    - path: "frontend/src/app/core/config/app-config.service.ts"
      provides: "Runtime config loading for backend endpoints"
      min_lines: 20
  key_links:
    - from: "frontend/src/app/core/auth/auth.interceptor.ts"
      to: "/api/"
      via: "Authorization Bearer header on app API requests"
      pattern: "Authorization"
    - from: "frontend/src/app/core/auth/milo-auth.service.ts"
      to: "frontend/src/app/core/config/app-config.service.ts"
      via: "uses configured API base URL"
      pattern: "AppConfigService"
---

<objective>
Create the frontend session layer that uses Milo-backed identity in this app.

Purpose: The backend trust path is not enough; the app also needs token persistence, auth-aware routing, and refresh-safe session bootstrap.
Output: Angular auth/config foundation with interceptor, guard, and a minimal auth bootstrap flow.
</objective>

<execution_context>
@C:/Users/davmelis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/davmelis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-milo-sso-foundation/01-RESEARCH.md
@C:/Users/davmelis/Documents/MyGitHub/Milo/frontend/src/app/core/auth/milo-auth.service.ts
@C:/Users/davmelis/Documents/MyGitHub/Milo/frontend/src/app/core/auth/auth.interceptor.ts
@C:/Users/davmelis/Documents/MyGitHub/Milo/frontend/src/app/core/config/app-config.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Angular runtime config and auth core</name>
  <files>frontend/package.json, frontend/angular.json, frontend/scripts/write-config.js, frontend/public/config.json, frontend/src/app/core/config/app-config.service.ts</files>
  <action>Create the minimum Angular application/config scaffolding required for runtime `config.json` loading. Follow the existing Milo/Symwriter pattern for API URL injection. Keep configuration focused on app API base URL and any Milo API pointer genuinely needed for auth handoff; avoid speculative config for later phases.</action>
  <verify>`npm test` or at minimum `npm run build` succeeds in `frontend/` once the module exists</verify>
  <done>The frontend can boot with runtime configuration rather than hardcoded API URLs.</done>
</task>

<task type="auto">
  <name>Task 2: Implement token persistence, interceptor, and route protection</name>
  <files>frontend/src/app/core/auth/milo-auth.service.ts, frontend/src/app/core/auth/auth.interceptor.ts, frontend/src/app/core/auth/auth.guard.ts, frontend/src/app/app.routes.ts, frontend/src/app/layout/main-layout.component.ts</files>
  <action>Implement an auth service that stores the Milo-backed token and current-user snapshot, restores them on startup, and exposes login state to guards/layout. Add an interceptor that attaches bearer tokens to `/api/` requests and clears state on 401. Add at least one protected route path so session restoration matters on refresh.</action>
  <verify>Frontend tests or build checks confirm the auth service compiles and the interceptor is registered correctly</verify>
  <done>Refreshing a protected app route preserves session state until the backend rejects the token.</done>
</task>

<task type="auto">
  <name>Task 3: Add minimal auth bootstrap or login bridge flow</name>
  <files>frontend/src/app/features/auth/login-bridge.component.ts, frontend/src/app/core/auth/milo-auth.service.ts, frontend/src/app/app.routes.ts</files>
  <action>Create the minimal UX needed for this app to receive or initiate Milo-backed authentication. If a dedicated Milo login page is not yet in scope, implement a bridge/entry flow that either redirects to Milo or accepts a token/session handoff pattern already used in the ecosystem. Keep the UI minimal and avoid building a second local credential form.</action>
  <verify>Manual or automated smoke check proves an unauthenticated user is directed into the Milo auth flow instead of a local login system</verify>
  <done>The app has a clear entry path into Milo-backed authentication and does not expose an independent local login implementation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` or equivalent frontend validation succeeds
- [ ] Token is restored from storage on refresh
- [ ] Interceptor adds bearer token and handles 401 logout path
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Frontend session behavior is consistent with Milo-backed auth and survives refresh safely
</success_criteria>

<output>
After completion, create `.planning/phases/01-milo-sso-foundation/01-02-SUMMARY.md`
</output>
