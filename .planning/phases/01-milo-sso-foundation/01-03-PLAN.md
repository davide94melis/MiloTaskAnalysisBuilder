---
phase: 01-milo-sso-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - backend/src/test/java/com/milo/taskbuilder/auth/MiloJwtServiceTest.java
  - backend/src/test/java/com/milo/taskbuilder/auth/TaskBuilderAuthenticationFilterTest.java
  - backend/src/test/java/com/milo/taskbuilder/auth/AuthControllerIntegrationTest.java
  - frontend/src/app/core/auth/milo-auth.service.spec.ts
  - frontend/src/app/core/auth/auth.interceptor.spec.ts
  - README.md
  - backend/README-deploy.md
  - frontend/README-deploy.md
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
user_setup:
  - service: deploy-config
    why: "Real auth smoke tests and deployment require consistent secret and API configuration across Milo and Task Builder."
    env_vars:
      - name: APP_JWT_SECRET
        source: "Same value used by Milo and sibling app backends"
      - name: API_URL
        source: "Task Builder frontend build environment"
must_haves:
  truths:
    - "Phase 1 behavior is covered by automated tests or explicit Wave 0 validation infrastructure."
    - "The repo documents how Task Builder joins the existing Milo shared-JWT ecosystem."
    - "The auth foundation can be executed and verified by later phases without rediscovering secret/config contracts."
  artifacts:
    - path: "backend/src/test/java/com/milo/taskbuilder/auth/AuthControllerIntegrationTest.java"
      provides: "Protected-endpoint and auth-path verification"
      min_lines: 20
    - path: "frontend/src/app/core/auth/milo-auth.service.spec.ts"
      provides: "Session restore verification"
      min_lines: 20
    - path: "README.md"
      provides: "Project-level auth integration notes"
      min_lines: 20
  key_links:
    - from: "README.md"
      to: "backend/README-deploy.md"
      via: "documents shared-secret and environment contract"
      pattern: "APP_JWT_SECRET"
    - from: "backend/src/test/java/com/milo/taskbuilder/auth/AuthControllerIntegrationTest.java"
      to: "backend/src/main/java/com/milo/taskbuilder/auth/AuthController.java"
      via: "integration tests protected identity endpoint"
      pattern: "auth/me"
---

<objective>
Harden and verify the Milo SSO foundation so later phases can build on it safely.

Purpose: Phase 1 should end with durable automated coverage and explicit configuration documentation, not only raw implementation.
Output: Auth tests, smoke verification, and environment/setup docs for the new app.
</objective>

<execution_context>
@C:/Users/davmelis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/davmelis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-milo-sso-foundation/01-RESEARCH.md
@.planning/phases/01-milo-sso-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add backend auth verification coverage</name>
  <files>backend/src/test/java/com/milo/taskbuilder/auth/MiloJwtServiceTest.java, backend/src/test/java/com/milo/taskbuilder/auth/TaskBuilderAuthenticationFilterTest.java, backend/src/test/java/com/milo/taskbuilder/auth/AuthControllerIntegrationTest.java</files>
  <action>Add tests for Milo JWT parsing, invalid-token rejection, first-login app-user bootstrap, and protected `/api/auth/me` behavior. Keep the tests aligned with the validation strategy and make them the primary quick feedback path for this phase.</action>
  <verify>`mvn test` passes with new auth coverage</verify>
  <done>Backend auth behaviors in Phase 1 are guarded by executable tests rather than manual confidence only.</done>
</task>

<task type="auto">
  <name>Task 2: Add frontend auth-session coverage</name>
  <files>frontend/src/app/core/auth/milo-auth.service.spec.ts, frontend/src/app/core/auth/auth.interceptor.spec.ts</files>
  <action>Add focused tests for session restoration from storage, Authorization header attachment, and 401-driven logout/reset behavior. Keep the test scope narrow and tied directly to AUTH-02.</action>
  <verify>`npm test` or equivalent frontend test command passes</verify>
  <done>Frontend session continuity and invalid-session handling are covered by executable tests.</done>
</task>

<task type="auto">
  <name>Task 3: Document the auth integration contract</name>
  <files>README.md, backend/README-deploy.md, frontend/README-deploy.md</files>
  <action>Document how this app joins the Milo ecosystem: shared `APP_JWT_SECRET`, backend/frontend config, auth bootstrap endpoint, and the explicit V1 rule that this app uses Milo only for SSO while keeping app data in its own schema. Keep the docs operational for future execute phases and deployment work.</action>
  <verify>Docs mention the shared-secret contract and the no-second-auth-system rule</verify>
  <done>The auth setup is reproducible for future work without re-reading sibling repos.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `mvn test` passes
- [ ] frontend auth tests pass or equivalent scaffolded test command is green
- [ ] docs clearly describe shared secret and Milo-backed auth boundary
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 1 ends with both implementation and validation/documentation support for the Milo SSO foundation
</success_criteria>

<output>
After completion, create `.planning/phases/01-milo-sso-foundation/01-03-SUMMARY.md`
</output>
