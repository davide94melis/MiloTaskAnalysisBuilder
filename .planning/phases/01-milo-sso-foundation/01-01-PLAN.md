---
phase: 01-milo-sso-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pom.xml
  - backend/src/main/resources/application.yml
  - backend/src/main/java/com/milo/taskbuilder/auth/MiloJwtProperties.java
  - backend/src/main/java/com/milo/taskbuilder/auth/MiloJwtService.java
  - backend/src/main/java/com/milo/taskbuilder/auth/TaskBuilderAuthenticationFilter.java
  - backend/src/main/java/com/milo/taskbuilder/auth/TaskBuilderPrincipal.java
  - backend/src/main/java/com/milo/taskbuilder/auth/SecurityConfig.java
  - backend/src/main/java/com/milo/taskbuilder/user/TaskBuilderUserEntity.java
  - backend/src/main/java/com/milo/taskbuilder/user/TaskBuilderUserRepository.java
  - backend/src/main/java/com/milo/taskbuilder/user/TaskBuilderUserService.java
  - backend/src/main/java/com/milo/taskbuilder/auth/AuthController.java
  - backend/src/main/java/com/milo/taskbuilder/auth/AuthMeResponse.java
  - backend/src/main/resources/db/migration/V1__create_taskbuilder_users.sql
autonomous: true
requirements:
  - AUTH-01
  - AUTH-03
user_setup:
  - service: milo-jwt
    why: "The backend must validate Milo-issued JWTs with the same shared secret used by Milo and sibling apps."
    env_vars:
      - name: APP_JWT_SECRET
        source: "Same secret already configured for Milo and Symwriter backends"
must_haves:
  truths:
    - "A request with a valid Milo JWT reaches protected Task Builder APIs as an authenticated app user."
    - "A first-time Milo user is bootstrapped into the Task Builder schema automatically."
    - "A request with a missing or invalid Milo JWT is not treated as authenticated."
  artifacts:
    - path: "backend/src/main/java/com/milo/taskbuilder/auth/MiloJwtService.java"
      provides: "Shared-secret validation and parsing of Milo JWT payloads"
      min_lines: 30
    - path: "backend/src/main/java/com/milo/taskbuilder/auth/TaskBuilderAuthenticationFilter.java"
      provides: "Bearer-token auth bridge from Milo JWT into Spring Security"
      min_lines: 30
    - path: "backend/src/main/java/com/milo/taskbuilder/user/TaskBuilderUserService.java"
      provides: "App-local user bootstrap and lookup from Milo identity"
      min_lines: 30
    - path: "backend/src/main/resources/db/migration/V1__create_taskbuilder_users.sql"
      provides: "Dedicated schema table for app-local authenticated users"
      contains: "taskbuilder"
  key_links:
    - from: "backend/src/main/java/com/milo/taskbuilder/auth/TaskBuilderAuthenticationFilter.java"
      to: "backend/src/main/java/com/milo/taskbuilder/auth/MiloJwtService.java"
      via: "filter delegates token parsing"
      pattern: "MiloJwtService"
    - from: "backend/src/main/java/com/milo/taskbuilder/auth/TaskBuilderAuthenticationFilter.java"
      to: "backend/src/main/java/com/milo/taskbuilder/user/TaskBuilderUserService.java"
      via: "filter resolves app-local user from Milo identity"
      pattern: "TaskBuilderUserService"
---

<objective>
Establish the backend trust boundary for Milo SSO in the new app.

Purpose: Everything else in the product depends on backend acceptance of Milo-issued identity and app-local authorization.
Output: Spring Boot backend auth foundation, app-local user bootstrap, and `/api/auth/me` style protected identity endpoint.
</objective>

<execution_context>
@C:/Users/davmelis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/davmelis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-milo-sso-foundation/01-RESEARCH.md
@C:/Users/davmelis/Documents/MyGitHub/Milo/docs/deploy-milo-auth-migration.md
@C:/Users/davmelis/Documents/MyGitHub/symwriter/backend/src/main/java/com/symwriter/milo/MiloJwtService.java
@C:/Users/davmelis/Documents/MyGitHub/symwriter/backend/src/main/java/com/symwriter/auth/filter/JwtAuthenticationFilter.java
@C:/Users/davmelis/Documents/MyGitHub/symwriter/backend/src/main/java/com/symwriter/auth/service/AuthService.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Spring Boot backend auth foundation</name>
  <files>backend/pom.xml, backend/src/main/resources/application.yml, backend/src/main/java/com/milo/taskbuilder/auth/MiloJwtProperties.java, backend/src/main/java/com/milo/taskbuilder/auth/MiloJwtService.java, backend/src/main/java/com/milo/taskbuilder/auth/TaskBuilderPrincipal.java, backend/src/main/java/com/milo/taskbuilder/auth/SecurityConfig.java</files>
  <action>Create the minimum Spring Boot backend structure needed for Milo JWT validation. Mirror the proven Symwriter pattern where useful, but name classes for this app and keep the scope limited to Milo JWT trust, app auth config, and protected API routing. Configure the shared secret through `APP_JWT_SECRET`; do not hardcode secrets or add a second login system.</action>
  <verify>`mvn test` or at minimum `mvn -q -DskipTests compile` succeeds in `backend/` once the module exists</verify>
  <done>Backend project compiles with auth config classes in place and protected routes can be wired through Spring Security.</done>
</task>

<task type="auto">
  <name>Task 2: Implement app-local user bootstrap from Milo identity</name>
  <files>backend/src/main/resources/db/migration/V1__create_taskbuilder_users.sql, backend/src/main/java/com/milo/taskbuilder/user/TaskBuilderUserEntity.java, backend/src/main/java/com/milo/taskbuilder/user/TaskBuilderUserRepository.java, backend/src/main/java/com/milo/taskbuilder/user/TaskBuilderUserService.java, backend/src/main/java/com/milo/taskbuilder/auth/TaskBuilderAuthenticationFilter.java</files>
  <action>Create a dedicated app-schema user table and service that resolves or creates a local user from a valid Milo JWT. Persist only the minimum needed in V1: local id, Milo user UUID, email, timestamps. The auth filter should parse the bearer token, validate it via `MiloJwtService`, bootstrap the local user, and populate Spring Security with an app-local principal.</action>
  <verify>`mvn test` includes service/filter coverage for valid token, invalid token, and first-login bootstrap behavior</verify>
  <done>Valid Milo JWTs create or resolve local users; invalid tokens do not authenticate requests.</done>
</task>

<task type="auto">
  <name>Task 3: Add authenticated identity endpoint for frontend bootstrap</name>
  <files>backend/src/main/java/com/milo/taskbuilder/auth/AuthController.java, backend/src/main/java/com/milo/taskbuilder/auth/AuthMeResponse.java, backend/src/main/java/com/milo/taskbuilder/auth/TaskBuilderAuthenticationFilter.java</files>
  <action>Add a simple protected endpoint such as `GET /api/auth/me` that returns the authenticated app user payload needed by the frontend. Keep the response small and stable. Use this endpoint as the canonical way for the frontend to validate restored sessions. Do not add registration/login endpoints in this app.</action>
  <verify>Authenticated integration test or controller test proves `/api/auth/me` returns 200 with valid Milo JWT and 401 without auth</verify>
  <done>The frontend has a protected bootstrap endpoint it can call to confirm the current Milo-authenticated session.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `mvn test` passes in `backend/` or an explicit compile fallback is green while tests are added in the same plan
- [ ] `/api/auth/me` is protected and succeeds only with valid Milo JWT
- [ ] First authenticated Milo access creates or resolves a local app user
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Backend trust boundary matches the established Milo/Symwriter shared-JWT pattern without introducing a second auth system
</success_criteria>

<output>
After completion, create `.planning/phases/01-milo-sso-foundation/01-01-SUMMARY.md`
</output>
