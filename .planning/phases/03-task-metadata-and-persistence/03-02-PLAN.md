---
phase: 03-task-metadata-and-persistence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/core/tasks/task-detail.models.ts
  - frontend/src/app/core/tasks/task-library.service.ts
  - frontend/src/app/features/library/task-shell-editor-entry.component.ts
  - frontend/src/app/features/library/task-metadata-form.component.ts
  - frontend/src/app/features/library/task-steps-draft-list.component.ts
  - frontend/src/app/features/library/task-shell-editor-entry.component.spec.ts
  - frontend/src/app/core/tasks/task-library.service.spec.ts
autonomous: true
requirements:
  - META-01
  - META-02
  - META-03
  - META-04
  - STEP-08
user_setup: []
must_haves:
  truths:
    - "The `/tasks/:taskId` route becomes a real metadata editor instead of a handoff placeholder."
    - "Users can edit and save task metadata through a dedicated detail contract."
    - "Ordered step drafts shown in the editor survive save and reload."
  artifacts:
    - path: "frontend/src/app/features/library/task-shell-editor-entry.component.ts"
      provides: "Phase 3 task metadata editor route"
      min_lines: 60
    - path: "frontend/src/app/features/library/task-metadata-form.component.ts"
      provides: "Dedicated form surface for task metadata editing"
      min_lines: 40
    - path: "frontend/src/app/core/tasks/task-detail.models.ts"
      provides: "Frontend detail contract separate from library cards"
      min_lines: 20
  key_links:
    - from: "frontend/src/app/features/library/task-shell-editor-entry.component.ts"
      to: "frontend/src/app/core/tasks/task-library.service.ts"
      via: "editor route hydrates and saves task detail payload"
      pattern: "updateTask"
    - from: "frontend/src/app/features/library/task-steps-draft-list.component.ts"
      to: "frontend/src/app/core/tasks/task-detail.models.ts"
      via: "ordered step drafts are rendered and submitted in explicit order"
      pattern: "steps"
---

<objective>
Turn the Phase 2 task-shell handoff screen into a real metadata editor with save/reload behavior.

Purpose: Phase 3 is only valuable if users can actually define the agreed metadata in the UI and trust that it persists across refresh.
Output: Task detail models, metadata form UI, ordered step draft display/editing, and save/reload behavior on `/tasks/:taskId`.
</objective>

<execution_context>
@C:/Users/davmelis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/davmelis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-task-metadata-and-persistence/03-RESEARCH.md
@.planning/phases/03-task-metadata-and-persistence/03-VALIDATION.md
@frontend/src/app/features/library/task-shell-editor-entry.component.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add frontend task detail models and service methods</name>
  <files>frontend/src/app/core/tasks/task-detail.models.ts, frontend/src/app/core/tasks/task-library.service.ts, frontend/src/app/core/tasks/task-library.service.spec.ts</files>
  <action>Introduce dedicated task detail models and client methods for loading and saving a full Phase 3 task. Keep the existing Phase 2 library-card contract intact for dashboard and library pages. Add service-level tests for detail fetch/update behavior.</action>
  <verify>`npm test -- --watch=false --browsers=ChromeHeadless` covers task detail client behavior</verify>
  <done>The frontend can talk to the new backend detail contract without disturbing Phase 2 dashboard/library flows.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the handoff screen with a metadata editor</name>
  <files>frontend/src/app/features/library/task-shell-editor-entry.component.ts, frontend/src/app/features/library/task-metadata-form.component.ts</files>
  <action>Convert `/tasks/:taskId` into a real editor page for title, category, description, educational objective, professional notes, target label, difficulty, environment, visibility, and support level. Keep the visual language aligned with the existing Milo-family UI and make save state understandable without becoming a heavy enterprise form.</action>
  <verify>Component tests or route-level tests confirm task detail loads into editable controls and save actions are wired</verify>
  <done>Users can edit the agreed Phase 3 metadata directly on the task page.</done>
</task>

<task type="auto">
  <name>Task 3: Add ordered step draft persistence to the editor surface</name>
  <files>frontend/src/app/features/library/task-steps-draft-list.component.ts, frontend/src/app/features/library/task-shell-editor-entry.component.ts, frontend/src/app/features/library/task-shell-editor-entry.component.spec.ts</files>
  <action>Render the ordered step draft payload on the editor page and ensure save/reload fidelity for that order. Add a minimal reorder mechanism for existing rows only, such as move-up / move-down controls, so `STEP-08` is actually user-verifiable in this phase. Do not introduce general step add/duplicate/delete authoring or broad step-content editing workflows; Phase 4 still owns core step authoring behavior.</action>
  <verify>`npm test -- --watch=false --browsers=ChromeHeadless` includes payload-order assertions plus editor tests proving existing step rows can be reordered and reloaded in the same order</verify>
  <done>The Phase 3 editor can persist and reload ordered step draft data exactly as configured, without absorbing the richer authoring scope of Phase 4.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test -- --watch=false --browsers=ChromeHeadless` passes in `frontend/`
- [ ] `npm run build` passes in `frontend/`
- [ ] Task metadata and ordered step drafts survive save and reload through the UI contract
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `/tasks/:taskId` becomes a usable metadata editor while still preserving the planned boundary with Phase 4
</success_criteria>

<output>
After completion, create `.planning/phases/03-task-metadata-and-persistence/03-02-SUMMARY.md`
</output>
