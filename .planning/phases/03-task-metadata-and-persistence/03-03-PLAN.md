---
phase: 03-task-metadata-and-persistence
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - backend/src/test/java/com/milo/taskbuilder/task/TaskDetailControllerIntegrationTest.java
  - frontend/src/app/core/tasks/task-library.service.spec.ts
  - frontend/src/app/features/library/task-shell-editor-entry.component.spec.ts
  - README.md
  - backend/README-deploy.md
  - frontend/README-deploy.md
autonomous: true
requirements:
  - META-01
  - META-02
  - META-03
  - META-04
  - STEP-08
user_setup: []
must_haves:
  truths:
    - "Phase 3 metadata and task-detail persistence are protected by cross-stack tests."
    - "The repo documents the new task detail contract and the boundary with later step-authoring work."
    - "Later phases can extend the editor rather than reworking its persistence contract."
  artifacts:
    - path: "backend/src/test/java/com/milo/taskbuilder/task/TaskDetailControllerIntegrationTest.java"
      provides: "Backend coverage for metadata save/reload and ordered steps"
      min_lines: 40
    - path: "frontend/src/app/features/library/task-shell-editor-entry.component.spec.ts"
      provides: "Frontend coverage for editor hydration and save behavior"
      min_lines: 30
    - path: "README.md"
      provides: "Updated Phase 3 contract documentation"
      min_lines: 20
  key_links:
    - from: "README.md"
      to: "backend/src/main/java/com/milo/taskbuilder/task/dto/TaskDetailResponse.java"
      via: "documents the new task detail payload"
      pattern: "task detail"
    - from: "frontend/src/app/core/tasks/task-library.service.spec.ts"
      to: "frontend/src/app/core/tasks/task-library.service.ts"
      via: "test coverage for detail load/save methods"
      pattern: "updateTask"
---

<objective>
Harden, verify, and document the Phase 3 task-detail persistence contract.

Purpose: Once metadata editing exists, the contract needs tests and documentation strong enough that Phase 4 can focus on authoring behavior rather than re-litigating persistence.
Output: Cross-stack coverage, updated docs, and a clearly verified boundary between Phase 3 persistence and Phase 4 authoring UX.
</objective>

<execution_context>
@C:/Users/davmelis/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/davmelis/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-task-metadata-and-persistence/03-RESEARCH.md
@.planning/phases/03-task-metadata-and-persistence/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete automated coverage for metadata save/reload behavior</name>
  <files>backend/src/test/java/com/milo/taskbuilder/task/TaskDetailControllerIntegrationTest.java, frontend/src/app/core/tasks/task-library.service.spec.ts, frontend/src/app/features/library/task-shell-editor-entry.component.spec.ts</files>
  <action>Add the missing tests that prove metadata editing and ordered step persistence survive round-trips cleanly. Favor stable contract assertions over brittle DOM detail checks. Include explicit step-order persistence assertions because `STEP-08` is phase-critical.</action>
  <verify>`mvn test` and `npm test -- --watch=false --browsers=ChromeHeadless` both pass</verify>
  <done>Phase 3 persistence behavior is protected by backend and frontend automated checks.</done>
</task>

<task type="auto">
  <name>Task 2: Update product and deploy documentation for the new detail contract</name>
  <files>README.md, backend/README-deploy.md, frontend/README-deploy.md</files>
  <action>Document the Phase 3 task detail payload, save endpoints, editor route behavior, and the explicit distinction between Phase 3 metadata persistence and Phase 4 step-authoring UX. Keep the docs short and implementation-faithful.</action>
  <verify>Documentation names the actual Phase 3 route and endpoint contracts introduced by the implementation</verify>
  <done>The repo communicates the persistence contract clearly enough for future work and deployment validation.</done>
</task>

<task type="auto">
  <name>Task 3: Run full verification and capture phase execution notes</name>
  <files>README.md, backend/README-deploy.md, frontend/README-deploy.md</files>
  <action>Run the full backend and frontend verification commands, capture any deviations, and ensure the plan summary records what was deliberately left for Phase 4.</action>
  <verify>`mvn test`, `npm test -- --watch=false --browsers=ChromeHeadless`, and `npm run build` are all green</verify>
  <done>Phase 3 can be executed and later verified without ambiguity about scope or persistence guarantees.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `mvn test` passes in `backend/`
- [ ] `npm test -- --watch=false --browsers=ChromeHeadless` passes in `frontend/`
- [ ] `npm run build` passes in `frontend/`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 3 leaves behind a stable and documented task-detail persistence contract for the later editor phases
</success_criteria>

<output>
After completion, create `.planning/phases/03-task-metadata-and-persistence/03-03-SUMMARY.md`
</output>
